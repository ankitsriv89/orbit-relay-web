{
  "name": "orbit-relay-web",
  "version": "1.0.0",
  "description": "Orbital Relay — interactive satellite visualization and tracking",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev public",
    "check:syntax": "node scripts/check/syntax.mjs",
    "check:resolve": "node scripts/check/resolve.mjs",
    "test": "npm run check:syntax && npm run check:resolve && npm --prefix workers/orbit-ingest test && npm --prefix workers/orbit-profiles test"
  },
  "keywords": ["orbit", "satellite", "space-track", "celestrak"],
  "author": "Ankit Srivastava",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "latest"
  }
}
