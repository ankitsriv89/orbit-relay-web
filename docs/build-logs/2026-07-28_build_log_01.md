# Build Log — 2026-07-28

| Time (IST) | Commit | Description |
|---|---|---|
| 00:35 | `3b6c14b` | **Plan 33 went live — schema applied, first real seed, and it caught a genuine bug immediately.** `d1/orbit.sql` applied to the remote `orbit-catalog` D1 (6 tables, clean). Repo secrets set (all 7), the `orbit-ingest.yml` `seed` job dispatched, seeded **31,629 objects**. `/api/search` and `/api/summary` came back correct — real totals, real regime/country/operator breakdowns. But every group bundle came out empty and `/api/tle?source=spacetrack` 404'd: **`TLE_LINE1`/`TLE_LINE2` were NULL on all 31,629 rows.** Root cause: a `predicates/` projection on `class/gp` with `format/json`, even one that explicitly lists `TLE_LINE0,TLE_LINE1,TLE_LINE2`, comes back with those three fields simply **absent** — not a 400, not empty strings. They are synthesized by Space-Track from the numeric elements rather than stored columns, so being listed in `modeldef/class/gp` never guaranteed they survive a narrowed projection. **Every one of 151 unit checks passed throughout**, because `fixtures/sample_gp.json` was captured as a full, unfiltered response and nothing in the suite exercised the shape actually sent in production — the gap between "the fixture is real data" and "the fixture matches what THIS query returns." Fix: `Q.gpDelta`/`Q.gpFull` no longer build a `predicates/` segment at all; GP always requests the full record. `GP_PREDICATES` stays exported and schema-tested. A new regression test calls `Q.gpDelta(GP_PREDICATES)` directly and asserts the argument is ignored, so a caller mistake can't reintroduce it. No data was lost — TLE lines backfill via the normal idempotent upsert on the next real GP ingest (the 6-hourly cron, `17 */6 * * *`). **152 unit checks.** |
| 02:05 | `46e8a17` | **Plan 33 wave 5 — derived conjunction screening.** Close-approach detection over the rendered slice, badged `UNOFFICIAL — NOT FOR COLLISION AVOIDANCE`: `public/orbit-engine/conjunction.js` (the maths, propagator **injected** so the driver is unit-testable in Node), `screen.worker.js` (a SECOND worker, and this repo's first **module** worker) and `screen-client.js`, plus a CONJUNCTIONS panel on `/spacetrack/`. Design calls worth keeping: the coarse capture gate is **derived** from the step (`threshold + 22.4·Δt/2`, where 22.4 km/s is twice surface escape speed) so the march provably cannot skip an approach — a *tuned* gate fails silently and looks like "there were no conjunctions"; refinement is a two-pass linear TCA from state vectors, which converges to **<1 mm and <1 ms** against closed-form geometry, so the test tolerances sit there rather than somewhere comfortable; the shell sieve compares **radii** derived from `satrec.a`/`ecco`, dodging the APOGEE-is-an-altitude trap entirely; and there is **no synchronous fallback**, unlike the propagation worker, because a main-thread screen would lock the page for ten seconds with no way to interrupt. No spatial broadphase on purpose — at a 500-object cap the ~125k squared-distance tests per step are cheap next to ~500 SGP4 calls, so the static sieve (which removes pairs *once*) is the part that pays. **The first real browser run changed the UI**: it reported `in 14m` for a pair closing at 6.5 m/s, where the separation is flat and the "TCA" is wherever rounding landed — such pairs are now labelled **co-orbiting**, and every row carries the **age of both element sets**, since SGP4 on a public TLE drifts kilometres within days and that, not the solver, is the error bar. **185 unit checks** (+25) and **91/91 E2E**, the load-bearing one re-propagating both objects on the main thread at the worker's reported TCA: agreement **0.0000 m**. |
| 03:40 | `e4d1d62` | **Mobile audit closed out — all 16 findings from `docs/mobile-audit-orbit-spacetrack.md` fixed on `/orbit/` and `/spacetrack/`, with three corrections to the audit itself.** Shipped: safe-area insets via four `--sa-*` vars (45 rules), `requestRenderMode` + a `requestRender()` call on all six scene-changing engine paths, `resolutionScale` 0.85 on phones, 44px touch targets, `@media (hover: hover)` guards, an `:active` state so touch keeps its press feedback, ≥11px type (16px on inputs so iOS stops zooming the page on focus), `touch-action: none`, tap-highlight off, `user-select: none` on panels, a 28px slider thumb, halved backdrop blur, focus rings, a landscape block keyed on `max-height`, `enterkeyhint`/`autocorrect` on the search field, and the topbar moved to the top of the DOM. **Three things the audit got wrong or that took a second pass, all worth keeping:** (1) **H1's premise was false** — measured at devicePixelRatio 3, Cesium already draws a 375×812 CSS canvas into a 375×812 buffer (ratio **1.0**, not 3.0) because `useBrowserRecommendedResolution` defaults true; the suggested fix `resolutionScale = 1/dpr` would have made it *worse* by cutting the buffer to a third. Pinned the flag as a guard and took the saving that actually existed. (2) **`max(px, env())` is the wrong safe-area idiom here** — these panels are stacked chips at deliberately distinct offsets and `max()` collapses the gap the moment an inset exceeds the smaller offset, putting one toggle back under another's body; `calc(base + inset)` shifts the stack and keeps the spacing. (3) **CSS cascade order beat the obvious fix** — overrides written into the main mobile block lost to `.refresh-btn`/`.source-btn` base rules declared *later* in the same file, so `.refresh-btn` stayed 26px; caught only because the browser check measured the element instead of trusting the rule. New `mobile_gate()` in `tests/e2e/test_orbit.py` runs 30 assertions in its own 390×844/DPR-3 context — the load-bearing one counts `postRender` events, because `requestRenderMode` fails by freezing the globe and nothing else in the suite would notice. **186 unit checks; E2E 91 desktop + mobile gate.** |
| 00:35 | n/a | **A duplicate workflow dispatch put two real GP full-catalog calls on the account within 26 minutes.** The CLI (`gh workflow run … -f job=seed`) was issued once, but `gh run list` showed a second `workflow_dispatch` run start two minutes later — most likely a second trigger via the Actions **Run workflow** button around the same time — plus a `schedule`-triggered run queued behind it once the `17 */6 * * *` cron became eligible mid-session (secrets went live partway through). Caught by checking `gh run list` rather than assuming the dispatched run was the only activity; cancelled the in-flight duplicate before its "Build the R2 artifacts" step could add more satcat/decay/boxscore calls, and cancelled the queued scheduled run before it started. Both completed GP calls returned 200 — no rejection or suspension signal observed — but recorded as a process note: **check `gh run list` before and after any manual dispatch of a job that spends external rate budget.** |
| 07:20 | `58175c6` | **Plan 33 wave 6 — the daily brief, and the plan's own "drop it if it reads as filler" clause shaped the whole design.** Narrative cards for `/spacetrack/`, generated **once a day inside the ingest** and stored as `brief/latest.json` in R2; `functions/api/brief.js` serves it as a flat object read. Nothing calls a model on a request path — visitor traffic must not be able to drive inference cost, and a per-request call would also make the card differ between two people on the same page on the same day. **Two decisions carry the wave.** (1) **The card is facts first and narrative second, so there is nothing to drop**: `collectFacts()` computes every number in SQL and the model is only ever asked to *phrase* them, so the artifact carries the facts whether or not a narrative survived and the panel renders them either way — with generation off, DAILY BRIEF is a digest of figures that are all still true. Droppability became a property of the artifact instead of a promise about a later decision. (2) **`checkNarrative()` rejects any narrative containing a numeral that is not in the facts it was given — including one the model computed correctly.** Three new objects out of five tracked really is 60%, and it is still rejected, because "did arithmetic" and "made it up" are indistinguishable from the output alone. The failure this wave is exposed to is not dullness, it is a fluent sentence with an invented figure sitting beside columns of measured data that lend it credibility, and nothing downstream would ever catch it — the same silent-plausible-output family as the GP-predicates bug, and the same shape of defence as wave 5's *derived* capture gate. Everything else follows: a rejection is written into `narrative_status` and shown as *narrative withheld — failed its fact-check* (silence would look identical to a quiet day); a genuinely quiet day is never narrated at all, because asking a model to describe nothing is where filler comes from; the narrative may not mention conjunctions or collisions, which is a **legal** rule rather than a stylistic one, CDM being the one class absent from USSPACECOM's basic-SSA set; the badge sits **above** the paragraph, as a provenance note read after the sentence is read too late; and the text is inserted with `textContent`, since "a model is unlikely to emit markup" is not a security boundary. `/api/brief` deliberately has **no D1 fallback** unlike `/api/feed` — rebuilding on a read would need inference, or would pair fresh facts with a sentence checked against older ones, silently breaking the exact property the gate holds; a test hands it a D1 that throws if touched. **Workers AI over Groq**, decided on the merits: one call a day from an Actions runner makes latency and cost irrelevant, which is most of what a faster host sells, so the tiebreak was operational surface and Workers AI adds none — but `src/brief.js` is written against the **binding interface**, so `scripts/ai-node.mjs` puts Groq behind the same `run()` with nothing above it changing, and a bigger model is available without leaving Cloudflare. **Gated off**: no model call happens anywhere unless `ORBIT_AI_CARDS` is set, and a test asserts the **Pages project does not bind Workers AI** — that absence is an invariant, not a configuration accident. `scripts/brief-preview.mjs` exists so the wave's exit criterion can actually be applied without turning it on in production: it prints the facts, the prompt and the gate's verdict, and `--n 5` shows the rejection *rate*. **A high rejection rate is a reason to change the model, never to loosen the gate.** **222 unit checks** (+36) and E2E extended with a brief gate at 1400×900 plus a three-chip overlap check at 390×844 — DAILY BRIEF is the third chip in the bottom-right slot and the offsets keeping them apart are hand-measured. |
| 08:55 | `3fd5fd8` | **Three real defects found while verifying wave 6, none of them by a failing test.** (1) **`brief.test.mjs` was seeding `DECAY_EPOCH` with `toISOString()`.** The class is `varchar(24)` upstream and real values arrive as `2026-07-26 04:12:00` — space separated, no zone — so the reentry horizon and the day countdown were being exercised against a shape Space-Track never sends, with all 31 checks green. Found by reading the fixture against `modeldef_decay.json`, which is **the same gap that let the GP `predicates/` bug through 151 green checks**: the data was real, but it was not data from the query under test. Caught earlier this time only because that entry exists to be re-read. Behind it sat two genuine bugs: the horizon compared a Space-Track epoch against a full ISO instant (two formats, correct only because `' '` sorts below `'T'`), now `substr(...,1,10)` against a date; and `daysUntil()` used a bare `Date.parse()` on a zone-less string, which V8 reads as **local time** — `2026-08-01 12:00:00` is 4.0 days out and returned **5** under `TZ=America/Los_Angeles`. Actions runners are UTC so production was safe; `brief-preview.mjs` and any non-UTC runner were not. Suite now runs green under UTC, `Asia/Kolkata` and `America/Los_Angeles`, and asserts the fixture *is* Space-Track's format so it cannot drift back to ISO and go green on a query that cannot work. (2) **`/api/decay-watch` had the identical local-time parse**, fixed rather than left because wave 6 put DAILY BRIEF directly above DECAY WATCH in the same corner listing the same objects — two adjacent panels disagreeing by a day about a reentry is a visible contradiction. `parseEpochUTC()` now lives once in `_catalog.js`. (3) **The mobile bottom chip stack had two overlaps that had already shipped**, found because the new E2E measures rects instead of asserting rules: `#results-hud` sat at the *same* 64px offset as `#signal-hud` and covered it **entirely** (one chip unreachable on every phone — they are in different corners on a desktop and only collide once the phone rules make both full-width), and `#signal-hud`/`#conj-hud` overlapped by 10px because a collapsed chip is **62px** on mobile (a 44px toggle — the audit's own touch-target minimum — plus 8px padding and borders) while the offsets kept the 52px step written when it was ~40px. The mobile audit broke its own "distinct offsets so none covers another's toggle" invariant when it grew the touch targets, and the CSS comment asserting "~40px" had been wrong ever since. Stack rebuilt as **64 / 134 / 204 / 274**; the E2E reads all four `getBoundingClientRect()`s and fails on any overlap. Nothing else could have caught it — every rule was correct, present and applied, and only the geometry was wrong. Same lesson as the `.refresh-btn` cascade finding: **measure the element, not the stylesheet.** **226 unit checks.** |
| 10:10 | `1bb5dba` | **Deployed, then curled production — and it found two more bugs, one of them mine and freshly shipped.** Neither was visible to any test. (1) **The epoch fix I had just deployed nulled every countdown on `/api/decay-watch` — 200 of 200 rows.** Space-Track sends `1957-12-01 0:00:00`, with an **unpadded hour**; my fix rewrote that to `1957-12-01T0:00:00Z` and handed it back to `Date.parse`, which is not valid ISO and returns NaN. The lenient parse I had replaced accepted it, so I traded a one-day error for a total loss. Root cause is *the same mistake as the commit before it, one layer down*: I generated the fixture instead of capturing one — `toISOString().replace('T',' ')` produces `00:00:00`, production sends `0:00:00`, and both sides of the comparison came out of the same formatter so the suite stayed green. Fixed by reading components with a regex and assembling with `Date.UTC`, never repairing a string for a lenient parser; tests now use strings copied out of a live response, and the two copies of the parser (ingest side and Pages side cannot import each other) are asserted to agree on every case. (2) **With the countdowns restored, the live watch list opened with SL-1 R/B, SPUTNIK 1 and SPUTNIK 2 at −25,076 days.** The `decay` table holds Space-Track's historical decay messages alongside its forward predictions and the query had only an upper bound, so a list captioned "ranked by soonest predicted reentry" was ranking 1957. **My new brief had the identical hole** and would have rendered `reentry ~-25076d` in the DAILY BRIEF panel, so this was a wave 6 bug as much as a wave 4 one. Both now floor the window at today's UTC date — *today* rather than *now*, so an object due in a few hours does not vanish before it has come down. Two existing decay fixtures were hardcoded 2026 dates, which against a date floor are a time bomb that passes until the wall clock overtakes them; they are relative now, and the already-decayed fixture's prediction was moved into the **future** on purpose so the new floor cannot be what excludes it and that test keeps proving the `DECAY_DATE` exclusion it is named for. Live after the fix: three real upcoming reentries (STARLINK-1719 in 0d, STARLINK-1666 in 2d, LVM3 R/B in 3d), zero null and zero negative countdowns. **232 unit checks** under UTC / `America/Los_Angeles` / `Asia/Kolkata`; **151/151 E2E**. Deployed to `signal-playground-0uj.pages.dev`. **The running lesson of the whole session: a green suite proves the assertions ran, not that they described production — three of the five defects here were found by reading a fixture against the schema or by curling the live site, and none by a red test.** |
