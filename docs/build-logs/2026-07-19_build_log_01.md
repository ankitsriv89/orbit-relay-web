# Build Log — 2026-07-19

| Time (IST) | Commit | Description |
|---|---|---|
| 04:34 | `45cb52b` | Mars Colony plan **23 — cloud save (BYOC): progress follows you via your OWN Google Drive** (4 commits, `3dbb50e`→`45cb52b`). Sign in on the globe hub and your per-site progress syncs to a `MarsColony-save.json` in *your* Drive — **pure frontend, zero backend**: Google Identity Services hands the browser a short-lived `drive.file` access token (no client secret in the token model) that calls the Drive REST API **directly**, so the save goes browser → your Drive and we never receive it (strong privacy, no storage liability). v1 = **progress only** (photos stay on the device). **`saves.js`** gains the serialise/merge core, all localStorage access kept in one module: `exportSite`/`importSite` (photos excluded; import writes verbatim so the merge-adopted `updatedAt` is **not** re-stamped), `exportAll`/`applyMerged` (the `{version,updatedAt,sites,prefs}` cloud shape), and a **pure `mergeSaves`** — **last-write-wins per site**, ties keep local (no write-back churn), null-safe — isolated because it is the one part that can *silently lose data*; **unit-tested 18/18**. New **`cloudsync.js`**: `createCloudSync()` lazy-loads GIS, does interactive `signIn()` + **silent `resume()`** on each hub mount (re-auth without a prompt while the Google session is live, else drop to signed-out), `signOut()` (revoke), and `syncNow()` = find → read → merge → apply-local → write-merged-to-Drive, with Drive REST v3 find/read(`alt=media`)/create(multipart)/update(media) and a one-shot 401 silent re-auth; only the signed-in *state* + expiry persist locally, **never the token**. **Sync is hub-centric** — the hub is short-lived and OPEN MISSION MAP is a full reload, so a persistent cross-page layer buys nothing; sync fires when the hub is alive (sign-in, each visit, tab-hide) and the per-site LWW merge makes that lossless. Hub UI: top-right **Sign in with Google** → SYNCING…/**SYNCED ✓**/OFFLINE chip + Sign out; a merge that adopts cloud progress refreshes the pin badges + open card. **Ships INERT** — hidden, no GIS script ever injected — until you paste an OAuth **Web Client ID** into `GOOGLE_CLIENT_ID` (a *your-action* step: Google Cloud consent screen + `drive.file` scope + authorized JS origins; no client secret; full recipe in plan-23 as-built). Known v1 limit (no tombstones): RESET SITE/GAME clears local, but while signed in the next LWW sync can restore the site from cloud — reset while signed out for now. No `functions/`/CSP change (project sets no CSP). E2E **29/29**: merge/export/import core 18/18 + cloud UI 11/11 (module inert-by-default with no GIS network, all four chip/button states via a no-OAuth preview hook). OAuth itself is not headless-testable → one manual two-device smoke test remains once the Client ID is set. |
| 04:25 | `601045e` | Mars Colony plan **22-B/C — the Site Hub: a rotatable 3D Mars globe as the first scene** (6 commits, `682ea25`→`601045e`). A new player now lands on a globe of real Mars — public-domain **USGS Viking MDIM 2.1** colorized mosaic (NASA Mars Trek WMTS, level-3 tiles stitched to 4096×2048 then downsampled to a clean 2048×1024 sRGB JPEG at `assets/hub/mars-globe.jpg`, ~0.6 MB) — and picks a landing site by its pin. New **`hub.js`** runs its *own* short-lived Three.js scene/renderer, disposed *before* `startGame`, so there is only ever **one WebGL context alive** (OOM-safe on the box the terrain clipmap already stresses); the globe is a single textured sphere with an additive-fresnel atmosphere rim, a warm starfield, slow auto-rotate + drag (Pointer Events unify mouse/touch, two-finger pinch-zoom, wheel dolly) on an outer-**tilt** / inner-**spin** rig so pins ride the surface. Each site is a pin at its real `center.{lon,lat}` mapped to the texture seam (`lonLatToVec3` — Gale 137°E / Jezero 77°E land correctly in the eastern hemisphere); playable sites = gold tappable pins carrying a name + progress badge (samples analysed/total + missions done, or NEW) read from `Save(id)`, future sites (new `LOCKED_SITES`: Hellas, Olympus, Meridiani, kept **out** of `SITES` so `startGame` can never route into an asset-less site) = dim "COMING SOON". Raycast picking uses an **analytic horizon test** (`dir·cameraDir > R/D`, one `isFront()` shared by pick + label visibility) so far-hemisphere pins — whose billboards ignore depth — are correctly non-pickable, robust right to the limb where comparing against the globe intersection was fragile. Tap → a docked card (name, mission, real coords, badge) with **ENTER/CONTINUE**; ENTER **flies the camera down into the pin + fades**, disposes the hub, then boots the sim whose landing-drop intro carries the descent. `boot()` rewritten to a 4-way priority: `?hub` → hub · `?site=` → deep-link · `mc-site` → auto-resume · first-ever → hub; `enterSite()` cleans the URL so a refresh auto-resumes. The in-game menu's per-site link list becomes one gold **OPEN MISSION MAP** button (→ `?hub`). Three named **reset tiers**: **RESET RUN** (hud rename, reload only) · **RESET SITE** (card, `Save(id).clear()`, arm/confirm, badge→NEW in place) · **RESET GAME** (hub corner, `resetGame()`, arm/confirm, all pins NEW). No infra/API change → no architecture/diagram update. **E2E 52/52** via the verify skill: globe render/decode/dispose (7), pins+cards+badges+pick+limb-occlusion (14), all four boot paths + fly-in (7), reset tiers + per-site save isolation (10), mobile touch-drag/pinch/safe-area (8), and a full sim round-trip confirming the RESET RUN rename + OPEN MISSION MAP → `?hub` (6). Front-runs Wave 2: every future site is now just a pin + its two asset files. |
