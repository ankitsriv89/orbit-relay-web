# Build Log — 2026-07-26

| Time (IST) | Commit | Description |
|---|---|---|
| 00:05 | `802b041` | **New site — Gusev Crater (Spirit · MER-A).** First of plan 30's six. 20m DTM + 6m ortho from the Mars 2020 landing-site survey — the same product pairing as Jezero, so the same 384-segment density and a 6km traverse worldSize. **AOI centered on the MIDPOINT of Spirit's mission arc, not its landing site:** centering on the landing clipped Columbia Hills off the east edge and gave only 78m of relief; re-centred, the crop holds both ends of the real 4.7km drive (Columbia Memorial Station NW, Husband Hill massif SE) with 114m. Hill positions were **derived by sampling the DTM for local maxima** rather than from published coordinates — and the resulting world frame reproduces Spirit's *published* landing coordinate exactly (HUD reads 14.56919 S, 175.47291 E at spawn), which is the real proof the projection chain is right. 13 samples of real MER-A science: Adirondack olivine basalt, Bonneville, Clovis goethite, Comanche carbonates, Home Plate's bomb sag, Troy's opaline silica, Tyrone's ferric sulfates, El Dorado. Highest `dustStorm` peak at the time (0.75) — Gusev's dust devils are what kept Spirit alive six years past its 90-sol design life. `test_gale_parity.py` generalized to **`test_site_parity.py`**, driven by a `SITE_MATRIX` so each new site inherits every invariant. **123/123.** |
| 00:12 | `31a10ee` | **New site — NE Syrtis (MRO survey, unflown).** Jezero's closest rival for Mars 2020, and the first site shipped that no lander has ever touched. AOI picked by **scanning the whole 37×46km DTM for the 6km window with the most relief**: 940m, the most vertical site in the game at the time. Verified drivable *before* committing to it — slopes median 7.3°, p90 18.3°, only 2.7% over 25° — so it reads as a big continuous ramp from the carbonate basin floor in the NW up onto the Syrtis Major lava plateau in the SE, not a cliff wall. Spawn sits low in the basin facing SE so W drives the whole climb. 13 samples following the real orbital stratigraphy: olivine-carbonate bedrock, serpentinized ultramafics, Isidis megabreccia, Fe/Mg smectites, layered sulfates, young Syrtis Major basalt capping the lot. **No landing to count sols from**, so the clock runs from MRO orbit insertion — the spacecraft whose CTX/CRISM data is the only reason the site is known. **163/163.** |
| 00:19 | `90794a7` | **New site — Elysium Planitia (InSight · Discovery 12).** The first site that is deliberately *not* dramatic: 67m of relief across 6km, the flattest we ship, because NASA picked Elysium for being the dullest ground on Mars — InSight was a stationary geophysics station and what happened here happened 3000km underneath it. AOI centred exactly on the lander, so **world (0,0) IS InSight** and you spawn on it. Finding the data took a detour worth recording: **these stereo DTMs are angled parallelograms, not filled rectangles**, so five of the seven products in the collection have their *bounding box* over the lander but **no data** at it. `gdallocationinfo` across all seven found the one real pair, which reports −2613.95m at the lander — a number the running game reproduces as ground −2613.65m under the spawned rover. 13 samples covering what InSight actually did: 1,300+ marsquakes on SEIS, the HP³ mole stalling at 40cm because Elysium soil cements into duricrust instead of collapsing, the liquid core at ~1830km radius, a 20–37km layered crust, the Cerberus Fossae quake bearing, S1222a, and the dust that ended it. **204/204.** |
| 00:26 | `aebfce3` | **New site — Meridiani Planum (Opportunity · MER-B); first REGIONAL site.** Meridiani has no high-res DEM — only the 200m HRSC/MOLA global blend — so a 6km crop would be 30 elevation samples across: a featureless blob. Built at **30km** instead, where the same DEM gives 150 samples *and* the crop spans Opportunity's real 14-year drive: Eagle Crater in the NW (world −10586,−9887) out past Victoria to the rim and 670m-deep floor of the 22km Endeavour Crater in the SE (+10551,+9899). 673m of relief, nearly all of it Endeavour. `prep_site.sh` grew two things for this: **per-source bounds** (`DEM_ULX/ULY/LRX/LRY`) because the blended DEM is in **degrees** while the CTX Robbins quads are Equirectangular Mars **metres** (`lon_0 = −180`); and **`ORTHO_SCALE`**, adding `-ot Byte -scale` for the 16-bit CTX quads — kept strictly opt-in, since an unconditional `-scale` would contrast-stretch Jezero's and Gale's 8-bit orthos into different albedo than they ship today. Imagery is the CTX **6m/px** equatorial quad (MC19), so texture is Jezero-grade even though geometry is 200m. Also fixed a latent hub bug: Meridiani sat in **both** `SITES` and `LOCKED_SITES`, and `hub.js` concatenates them into one pin array, so it would have rendered **two overlapping globe pins** — now removed and guarded by an E2E check. **246/246.** |
| 00:31 | `a4d7e7b` | **New site — Olympus Mons (Mariner 9 survey, unflown).** The most spectacular thing in the game: 90km square framing the entire nested caldera complex, **3407m of wall**, and the first site with **POSITIVE elevations** (+17.6 to +21.0 km) because it is the summit of the largest volcano in the solar system. **Finding the centre took three tries and is the lesson here:** the textbook coordinate (133.8W, 18.65N) put the caldera in the SE corner and clipped it; taking the DEM's global maximum landed on a flank high at (133.06W, 17.31N), ~90km away; what actually worked was **rendering the DEM's relief to PNG and measuring the depression directly** — (133.1678W, 18.3271N), complex ~78km across, framed at 90km so the rim sits just inside. At 200m DEM that is 450 samples across, the one regional site where the global blend genuinely matches the landform's scale; `segments` 512 = 176m quads, deliberately matched to the DEM rather than finer. 13 samples of real volcanology: caldera-floor lavas barely cratered, six overlapping collapse craters each from a magma chamber foundering, terraced walls, wrinkle ridges from the floor sagging as it cooled. **The thin air drives a gameplay inversion:** `dustStorm` peak **0.35**, the *lowest* of any site, because at a tenth of datum pressure there is barely enough atmosphere to lift dust — against Meridiani's **0.9**, where a global dust storm killed Opportunity. |
| 00:34 | `f9dbb21` | **New site — Hellas Planitia (MGS survey, unflown); `LOCKED_SITES` now EMPTY.** Mars Colony goes from **2 playable sites to 8**. **Relocated** from the old locked pin at (70E, 42.4S), which has no CTX coverage at all and could only ever have shipped a Viking-resolution blur — the Hellas exploration-zone mosaics sit at 99.66–106.28E / 37.32–41.81S, so the site moved onto real 5m imagery: 30km square at (102.97E, 39.56S) on the basin's eastern rim, with ortho data verified at all four corners *and* the centre before baking. The only site that **crosses the datum**: −694m on the basin side up to +2574m on the rim highlands, 3268m of relief — yet the *gentlest* regional site (slopes median 2.5°, p90 14.1°, 1.6% over 25°), because that relief is a long regional climb rather than a wall. Unlike Meridiani/Olympus both sources here are already in degrees (the CTX_EZ mosaics are, unlike the Robbins quads), so one set of bounds does both and its Byte ortho needs no `ORTHO_SCALE`. 13 samples: basin-floor fill, the rim scarp's 3km section, dendritic valleys cut when Mars still had weather, Noachian highlands, viscous-flow glacial aprons, impact melt, the honeycomb terrain unique to this basin, young gullies. `dustStorm` 0.85 — Hellas is one of the planet's storm nurseries. |
| 00:36 | `a96e23f` | **Hub globe labels no longer get sliced by the planet.** A pin's label sprite is centred on its pin, so for any pin near the limb the far half sat *behind* the globe surface and depth-testing cut it mid-word — "Olympus Mons" rendered as "Olympus Mor". Pre-existing, but invisible until plan 30 put a site under the default camera rotation with a name long enough to reach the horizon. Fixed by disabling `depthTest` on the label material; nothing is lost, because labels for pins over the horizon are **already** culled explicitly by the `isFront()` test at `hub.js:586` — depth testing was only ever re-doing that job badly for the pins it could actually see. Verified 8/8 pins render, unique, all playable, no console errors, full name drawn. |
| 17:38 | `92aadec` | **Orbital Relay wave 0 — the #71 memory design finally back-ported.** Plan 33 opens here because the standalone `/orbit/` was *behind* the marsapiens copy: the June sync copied the enhanced page **into** marsapiens, the `PointPrimitiveCollection` fix was then made *there only*, and it never came back. So the standalone still gave every satellite its own Cesium Entity with a `CallbackProperty` position — the design behind the parent's **1.2 GB** heap blowup, re-running SGP4 for every sat every frame (~36k propagations/sec at 600 sats × 60fps) on top of per-Entity property-bag overhead. Adding Space-Track's ~28k objects on top would only have fed the leak faster, which is why this had to land before any ingest work. Now one shared collection with positions written in place by a throttled tick off the Cesium clock; time-warp still works because the clock is still the single time source. Also fixed **iridium-NEXT never using its shipped baseline**: `fetchTLE` lowercases the group slug but the file on disk was mixed-case, so it 404'd silently and always took the slow API path. **New E2E `tests/e2e/test_orbit.py` — this repo had no orbit tests at all.** 17/17. |
| 18:21 | `9d84e8a` | **Orbital Relay wave 1 — SGP4 moved into a Web Worker.** New `public/orbit/worker/propagate.worker.js`. Per-tick propagation returns **one transferable `Float32Array`** of ECEF metres which the main thread hands straight back via `recycle`, so a steady tick allocates nothing on either side. Orbit rings and ground tracks became worker jobs too — those were 90-120 synchronous propagations each, at load and again every 2s per `CallbackProperty`, which is audit finding **M-18**. Measured at 625 visible sats: **53.7 ms** of main-thread SGP4 per tick → below `performance.now()` resolution. The worker computes geodetic→ECEF with the same formula as Cesium's `Cartographic→Cartesian` rather than round-tripping through `fromDegrees`, so points land exactly where they did; agreement with the sync path is **0.10–0.20 m**, pure Float32 quantisation at Earth's radius. It is an optimisation and never a dependency — dropped on error, in-flight path jobs settled from their fallbacks, and the suite **kills the worker deliberately** to prove the globe keeps moving. Perf gate (5 min, 625 visible / 1215 tracked): heap **83.8 → 78.5 MB**, flat. Frame rate deliberately not asserted — SwiftShader runs at ~0.3 fps regardless, so an fps threshold would measure the rasteriser, not the change. Also fixed the *source* of the iridium bug: `snapshot_tle.sh` wrote the mixed-case filename and would have reintroduced it. **27/27.** |
| 18:22 | `fa5aed6` | Plan 33 status + issues log. Records the 23.7 km "coordinate bug" that was really fixed-sleep sampling error: at 0.3 fps the main thread goes seconds without running the handler that applies worker positions, so `time.sleep()` reads a stale coordinate and the ISS's 7.66 km/s does the rest. Tests now sync on a `tickCount` the page exposes. |
| 19:24 | `46a2552` | **Corrected the plan's wrangler prerequisites.** `wrangler secret put` is Worker-scoped, not account-scoped — the block as written would have attached the Space-Track credentials to the **Pages project**, the one place they must never live, and the `orbit-ingest` Worker they belong to does not exist until wave 2. Split into step 1 (account resources, now) and step 2 (secrets, after wave 2 deploys the Worker). Also records that Space-Track has **no API key at all** — those secrets are the login credentials — and that test-server access is conditional rather than required. |
| 19:53 | `d404f33` | **Bound `orbit-catalog` D1 + `orbit-data` R2 to the Pages project.** Read side only; the ingest Worker carries its own copies for the write side. **`ORBIT_KV` deliberately NOT bound here** despite existing: it holds the live Space-Track session cookie, only the Worker touches it, and binding it into Pages would hand every Pages Function a live credential for no reason. Binding names normalised to `ORBIT_DB`/`ORBIT_R2` to match `ORBIT_KV` and the existing `TELEMETRY_DB`. |
| 23:07 | `b420e33` | **Wave 2 part 1 — D1 schema + Space-Track client with budget guard.** The parts that touch Space-Track zero times, so approval for the test server never blocked progress. `d1/orbit.sql`: objects / satcat / decay / boxscore / events / api_calls, all idempotent so the file is re-runnable as a migration. `workers/orbit-ingest/` is a separate Worker because **Pages Functions cannot run cron triggers**. `src/spacetrack.js` funnels every outbound request through one `query()` so the rate ceiling is enforced in a single place; auth is username/password (there is no API key), cookie cached in KV and expired early at 90 min so no job starts on a cookie about to die, and a dead cookie triggers **exactly one** forced re-auth rather than a loop. The **budget guard hard-aborts at 25 calls in any rolling hour** against a ~8/day steady state, and calls are logged **before** they are sent, so a request that throws still consumes budget — a crash that skipped the log would let a retry loop spend the whole allowance invisibly. `SPACETRACK_BASE` makes the test server a one-variable switch. **12/12 unit tests, no network and no wrangler**; the guard is asserted against the plan's own fixture — a loop that would make 40 calls aborts at exactly 25 with no 26th request sent. |
| 23:30 | `a23c5a7` | **Schema corrected against Space-Track's own `modeldef`.** The captured exports caught three real errors in the hand-written schema, none of which would have surfaced until the first live ingest silently wrote NULLs: GP calls its apsis fields **`APOAPSIS`/`PERIAPSIS`**, not `APOGEE`/`PERIGEE` (SATCAT uses the other pair, which is where the confusion came from); `satcat` declared **three columns that do not exist** (`OPS_STATUS_CODE`, `ORBIT_CENTER`, `ORBIT_TYPE`); and `objects`/`decay` were each missing real fields. Also settles a question the PDFs never answer: **GP's apsides are ALTITUDES above the surface, not radii** — verified two ways, `SEMIMAJOR_AXIS − 6378.137` equals their mean exactly *and* GP agrees with SATCAT sub-km for the same objects. Wrong, and the ingest would have placed the ISS at ~6800 km and taken every orbit-regime classification with it. `test/schema.test.mjs` makes it permanent by parsing `d1/orbit.sql` and failing on any column absent from the captured modeldef. Fixtures committed (96 KB) so no test in this Worker needs the network. **12/12.** |
