# Build Log — 2026-07-20

| Time (IST) | Commit | Description |
|---|---|---|
| 23:00 | `5060c9c` | **Mars Sim — walker rename + polish.** Renamed the plan-25 walkers **Strider→Ongak** (quadruped) and **Arachne→Makadane** (octopod) after a company-name clash on "Strider": `strider.js`→`ongak.js` / `arachne.js`→`makadane.js` (`createOngak`/`createMakadane`, `SPEC.name`), every `main.js` ref (imports, `units[]`, colliders, `__mc.ongak`/`__mc.makadane`, shadows, boot updates), `hud.js` (dossier catalog keys/titles/blurbs + CONTROLS), `walker-rig.js` header, and the concept assets (`dossier/{ongak,makadane}.jpg`). **Visual polish** (they read too procedural): `walkerMaterials()` now takes a per-unit `livery {panel, accent, glow}` (lower roughness / higher metalness for crisper spec), plus a new **emissive `glow` trim material** and brighter emissive lens. Distinct identities — **Ongak = warm** (burnt-orange + amber sensor glow: glowing flank strips, face sensor bar, instrument cap, hull greebles) and **Makadane = cool** (teal + cyan glow: glowing rim ring, mast sensor bar, antenna-tip beacons) — so the two no longer look identical. Verified headless: renamed `__mc` handles present (old gone), 44/64 meshes, glow renders, **0 console errors**. |
| 22:45 | `a302ba0` | **Mars Sim — plan 26 grounded graphics pass (partial) + gameplay fixes.** Plan 26 (`docs/game-plans/26_grounded_graphics_pass.md`): vendored the r185 **post-processing addons** (`vendor/three/addons/{postprocessing,shaders,math}`, 15-file offline-verified closure) and built a full **sun-shadow pipeline** (moving ortho frustum, caster sweep, hand-rolled terrain PCF sampler) — but **gated OFF** (`SHADOWS=false`) because cast shadows don't render under SwiftShader (headless GPU) and couldn't be confirmed on real HW; flip to `!QUALITY.coarse` to test. Shipped: subtle **screen-space colour grade + vignette** (CSS canvas-filter + `#mc-vignette` overlay, the same route night-vision uses — no EffectComposer needed). Gameplay: **removed the humanoid EVA tether** (mobile repair van makes it obsolete — dropped the anchor selection, speed-clamp, taut banner, help text); verified **van driving works** (moves at maxSpeed when driven; board via **E** near the van — discoverability, not a bug); confirmed the humanoid renders fine locally (rigged GLB, legs present — the "invisible lower half" is a stale deploy/cache, resolved by this redeploy + hard-refresh). 0 console errors. |
| 14:35 | `69751ab` | Mars Colony plan **25 — legged walker units: "Strider" (quadruped) + "Arachne" (octopod)** (4 commits, `6528bb6`→`69751ab`). Two new **fully procedural** ground units (no GLB, no asset pipeline) built on one shared **`walker-rig.js`** engine that generalizes the Wave-12 humanoid foot-IK from 2 hardcoded bone-legs to an **N-leg array** on procedural `Object3D` chains (hip-yaw → femur → tibia). The engine does two-bone analytic IK per leg with an optional **coxa yaw** joint so radially-splayed legs (octopod) sweep fore-aft while sagittal legs (quad) skip it — the arthropod/hexapod trick. Unlike the biped (gravity-vertical), the walker **body tilts with the terrain** (`sampleGroundNormal`) — the legged-robot signature. **`strider.js`** = 4 sagittal legs, diagonal-pair trot, X-stance knees (front bulge forward / hind back), high slope tolerance (`slopeK` 0.45); **`arachne.js`** = 8 radial legs, alternating-tetrapod gait (four feet always planted) with a metachronal ripple, near-slope-proof (`slopeK` 0.3). Procedural livery matched to the concept renders: white composite panels + burnt-orange accent bands + gunmetal links + bronze actuators + an emissive sensor lens (proves procedural ≠ flat-white). Wired into `main.js` (`units[]`, colliders, shadows, `__mc` handles, boot updates); **HUD dossier**: a chip beside the active walker's name + a **FLEET DOSSIER** menu section, both opening a full-screen **lightbox** of the concept render (`assets/dossier/{strider,arachne}.jpg`, downscaled ~150 KB each), plus CONTROLS lines. **Bug found + fixed in build:** the humanoid's `swingT=(sin+1)/2` swing resolves to `lift=swingLift·cos(s·π/2)` — lift **peaks at the stance transitions and zeroes mid-swing** (inverted), and the swing arc never meets the plant point, so on a multi-leg body the foot **slams ~1.1 m horizontally at touchdown** (the biped hid it via world-locked stance feet). Replaced with a **continuity-planted swing**: the swing lerps from the lift-off point to a `stepHalf`-ahead home over a correct mid-swing `sin()` lift, and the stance plant is captured from the foot's **actual commanded position** at touchdown → skate is impossible by construction. Verified **20/20** headless (verify skill): rigs assemble (37 / 59 meshes), 4 / 8 legs, both travel (8 m / 6 m in 4 s), planted-foot slip → 0, bodies terrain-grounded, Tab-cycle + dossier chip visibility + lightbox open/close + menu cards. No `functions/`/infra change. |
