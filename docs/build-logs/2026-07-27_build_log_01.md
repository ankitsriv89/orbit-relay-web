# Build Log — 2026-07-27

| Time (IST) | Commit | Description |
|---|---|---|
| 04:35 | `2d51d57` | **Orbital Relay wave 2 — the Space-Track source is live.** The `disabled title="Phase 2"` is gone and `/api/tle?source=spacetrack` reads the R2 bundles the new `orbit-ingest` Worker maintains. Five ingest modules (`gp`, `satcat`, `decay`, `boxscore`, plus `derive`/`operators`), cron dispatch in `src/index.js`, and `scripts/bootstrap.mjs` for the one-time ~28k-object seed. **The design changed in one important way once the delta was sized honestly:** a 6.7-hour `CREATION_DATE` window does not return a few hundred rows — 18 SPCS regenerates elsets continuously, so *most of the active catalog* has a fresh elset in any 6-hour window, and the response is 10–20 MB. `await resp.json()` would have held the text **and** ~28k parsed objects at once, past a Worker's 128 MB limit. New `src/jsonstream.js` scans the JSON array incrementally and hands each element to the real `JSON.parse`, so resident memory is one row plus the current 40-row upsert batch **regardless of catalog size** — asserted by feeding the parser one byte at a time and by proving it yields a row while the stream is still open. Second real trap: **Space-Track sends every scalar as a JSON string** (`"PERIOD": "132.594"`); stored uncoerced, `WHERE PERIOD BETWEEN 1430 AND 1450` is a *string* comparison and the GEO group silently returns nothing — now coerced at ingest and asserted via `typeof(PERIOD) = 'real'` in a real SQLite. All 20 legacy group slugs became SQL predicates over our own mirror, so switching source changes nothing else in the UI; the four debris groups are `OBJECT_ID` prefix matches (a fragment catalogued next year joins its family automatically) and `last-30-days` is a live date predicate, both **better** than the curated Celestrak lists, while `glo-ops` and `sbas` are flagged `approximate: true` because GLONASS payloads are catalogued as `COSMOS nnnn` and can only be found by orbit signature. **New `test/sqlite.test.mjs` applies `d1/orbit.sql` to an in-memory `node:sqlite` and runs the real statements** — that is what proves the 20 predicates compile *and* match a specimen, that a second ingest upserts rather than duplicating, and that `first_seen` survives it. **108 unit checks, no network, no wrangler.** |
| 04:35 | `2d51d57` | **Enabling the button exposed a real bug in the switch itself.** Selecting a source called `reloadAllLayers()`, which only re-fetches the *optional* layer checkboxes — the ISS, the other stations and the Starlink batch are built once by `loadSatellites()` and live outside `layerState`. An in-place switch would therefore have left most of the globe showing **Celestrak points under a "Live data: Space-Track" label**, which is both wrong and an attribution problem. The switch now re-boots the page and reads the choice back from `localStorage`. Also: Space-Track is **API-only** by design — no static baseline file is shipped or requested for it, because a Space-Track `.txt` sitting in the repo would be redistribution without the visible citation the approval is conditioned on. The citation itself is now in the DOM, in the R2 object metadata, and on the `X-Data-Source` response header, with a test asserting the Worker and Pages copies of the string are byte-identical. **E2E 36/36** (7 new checks); the source-switch test needed its own trap noted — `location.reload()` destroys the execution context, so a `page.evaluate` issuing the click races the navigation and throws. |
| 18:12 | `73e7681` | **Wave 2b — the ingest leaves Workers for GitHub Actions.** The GP job parses and upserts most of the ~28k catalog in one invocation: ~300 ms of CPU against Workers Free's 10 ms. Streaming fixed the *memory* ceiling, not this one, and cadence cannot fix it either — CPU is driven by rows/day, which is fixed, so even 1-hourly (the fastest GP rate Space-Track permits) is still ~55 ms. **Not a rewrite:** `src/*` is written against a deliberately narrow slice of the bindings (`prepare().bind().run()/all()/first()`, `batch()`, R2 `put()/get()`) — which is exactly why `test/fakes.mjs` could ever stand in for them — so `scripts/env-node.mjs` reimplements that slice over the public APIs and **every ingest module is untouched**. `ORBIT_DB` → the D1 HTTP API, `ORBIT_R2` → the R2 S3 API with hand-rolled SigV4, `ORBIT_KV` → a Map (nothing is lost: the cookie cache exists to share a login across invocations and an Actions run is one process; the SATCAT cursor already fell back to `SELECT MAX(FILE) FROM satcat`). **The one decision worth recording: parameters are inlined as SQL literals rather than sent in the REST API's `params` array, because that array is typed as *string*.** Space-Track already sends every scalar as a JSON string and `derive.js` coerces them precisely so `WHERE PERIOD BETWEEN 1430 AND 1450` compares numbers — handing those numbers back to a string-typed parameter array would undo that coercion at the last hop, and silently, since the symptom is wrong rows rather than an error. `batch()` maps onto the API's multi-statement form, so a 40-row upsert batch is one request (~700 round trips for a full delta rather than ~28,000). **SigV4 is pinned to reference signatures generated by botocore**, not to itself, and R2 needs its own `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY` because SigV4 cannot consume a Cloudflare API token. The citation string is asserted **ASCII** — it ships as an S3 metadata header, where a curly quote would throw inside `fetch()` and take the whole artifact rebuild with it. Daily crons moved to 17:35/17:40 (GitHub cron is UTC and best-effort *late*), serialised by `concurrency: orbit-ingest` — two concurrent runs would race each other's upserts *and* both spend against a budget whose ceiling is an account suspension. `wrangler.toml`'s crons and `scheduled()` stay in the repo, deployable and unused, as the fallback. **131 unit checks** (was 108). |
| 18:40 | `e51378d` | **Wave 3 — the engine got extracted and Space-Track got its own page.** The PointPrimitiveCollection, the worker plumbing, transferable buffers and `fetchTLE`/`parseTLE` were ~600 of `orbital-relay.js`'s ~1,100 lines, and **that engine had already been fixed twice in two copies** — the marsapiens/standalone divergence that opened plan 33, about to happen inside one repo. It now lives once in `/orbit-engine/` (`astro.js`, `tle.js`, `sat-engine.js`, `propagate.worker.js`, the vendored `satellite.min.js`), with both pages as ES modules on top. **The worker URL is absolute on purpose:** a relative one resolves against the *page*, so `/spacetrack/` would have looked for `/spacetrack/propagate.worker.js`, failed, and silently fallen back to synchronous SGP4 — a performance cliff with no error anywhere, which is why the E2E asserts the worker is live *on the new page*. `/spacetrack/` renders arbitrary catalog slices: filters → `/api/search?...&tle=1` → points coloured by object type, click → `/api/object/:norad` → a dossier joining GP, SATCAT and the decay predictions. The dossier resolves GP's `APOAPSIS`/`PERIAPSIS` and SATCAT's `APOGEE`/`PERIGEE` into one `apogee_km`/`perigee_km` pair, **both altitudes**, so the frontend cannot get it wrong; the ISS is identified by NORAD **25544**, never a name match (**M-19** — "ISS (NAUKA)" and "ISS DEB" both match a substring test). `operator` is labelled **derived** everywhere, because a filter that looks authoritative and is not is worse than one that is absent. On `/orbit/`, SPACE-TRACK became an `<a>`, which **retires the wave-2 `location.reload()`** — needed because the ISS, the other stations and the Starlink batch are built outside `layerState` and could not be re-sourced in place — and the E2E dance around a destroyed execution context with it. The extraction changed no measurement: worker-vs-sync agreement **0.215 m**, main-thread tick cost **82.8 ms → 0.40 ms**, heap flat at 625 visible. **E2E 50/50** (was 36). |
| 18:52 | `f2d845a` | **The new catalog endpoints got a real test, and it found one.** The E2E runs against a static server with no Pages Functions, so it could only prove `/spacetrack/` fails soft; `test/pages-api.test.mjs` applies `d1/orbit.sql` to `node:sqlite`, seeds from the fixtures and calls `onRequest` directly. It lives in the ingest package because those Functions are the **read** side of the schema that package owns the write side of — one applied schema, both sides checked, rather than a second copy of the harness. The find: `Number.parseInt` accepts trailing garbage, so `/api/object/25544;%20DROP%20TABLE%20objects` parsed to 25544 and answered **200 with a real dossier**. Never an injection — the value is bound — but a malformed path returning a plausible record hides the caller's bug; the whole segment must now be digits. Also flipped the wave-2 assertion "the source button is no longer disabled" to assert the *opposite* shape: that a `<button class="source-btn">` has **not** come back, which is exactly what a regression to the in-place switch would look like. **151 unit checks.** |
