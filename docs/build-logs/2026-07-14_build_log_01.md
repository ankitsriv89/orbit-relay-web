# Build Log — 2026-07-14

| Time (IST) | Commit | Description |
|---|---|---|
| 04:25 | `f67ae9d` | Mars Colony Wave 9.8 — **next-step guidance on every mission step** (#10). The objective banner had always shown exactly one step and swapped it in **silently**: complete a step and nothing confirms it — you had to *notice* the bottom line had changed to know you'd done anything. `missions.js`'s `advance()` gains an `onStep` broadcast carrying the step it just crossed *and* the one that follows (still announce-only — no game logic moved into that module), and the HUD gets a transient banner naming both: "✓ STEP 3/7 — PRESS TAB TO SWITCH TO THE LIFT DRONE" over "NEXT · 4/7 · HOVER LOW OVER THE CACHE…", which then settles into the persistent objective line. On the last step it reads the mission **complete** rather than previewing a step that doesn't exist. It takes its **own slot** (4th in the top banner stack), *not* the build toast's — both fire in the same breath (analyzing a sample completes a step **and** raises a checkpost) and one shared slot would have had one announcement silently eat the other; amber "what to do next" against the toast's teal "this happened". Skipping a mission stays silent (`skip()` finishes without walking steps). No sound cue of its own — every step fires on an action that already has one, and a second chime on the same frame just reads as a double-tap. E2E 38/38: walked the full 7-step chain — each completion names the right step and previews the next *verbatim* as the objective banner then shows it; at the analyze step guidance (top 176 px) and toast (top 136 px) both render without overlapping. |
| 04:00 | `c73237b` | Mars Colony Wave 9.7 — **night vision: an image intensifier for driving after dark** (#12). At `daylight() = 0` the terrain sits around **12/255** — there was no way to travel at night but wait for sunrise. Done as a **CSS filter on the canvas**, not a post-processing pass: the project has no EffectComposer (`effects.js` is deliberately-unlit fakes) and a render-target pipeline is a heavy way to pay for one toggle. Filtering the canvas *element* also leaves the HUD untouched — telemetry and minimap stay in their own colours, exactly like a real NVG rig (the optic is intensified, the instruments are not). **Two bugs the pixel measurements caught that no DOM assertion could:** (1) the first chain had `contrast(1.15)`, which *silently defeated the entire feature* — contrast maps `v → (v − 0.5)·c + 0.5`, so a lifted night pixel (0.067) lands back at **0.002**, blacker than it started; every computed-style check passed while the screen stayed black. Dropped. (2) `--nv-gain` is now the tube's **automatic gain control**, driven from `env.daylight()`: wide open in the dark (**10×**), stopped down at noon (**1.6×**) so leaving NV on in daylight gives a bright green view instead of a white blowout — real intensifiers do exactly this; the chain stays in CSS, only the scalar crosses over. Two entry points, one state: top-bar **NV** button (+ **N** key) and a menu toggle. The top-bar button is **desktop-only** — measured at 390 px the bar already runs to 335 px of the 366 px available and a fifth button pushed SFX clean off the screen (to 407 px); phones toggle from the menu, and a proper mobile home comes with Wave 10's reshuffle. E2E 28/28: a real night frame goes **R12 G6 B4 → R32 G78 B22** through the live filter chain (6× brighter, phosphor-green, navigable); daylight clips just 1.9 % of pixels. |
| 03:30 | `4e5edab` | Mars Colony Wave 9.6 — **Mars clock: sol + local time-of-sol** (#9). The sim's day is *compressed* (`environment.js` runs a 40-real-minute sol, not Mars' true 24h 39m 35s), so simulating real sol length would desync from the sun you can actually see and leave the game with two clocks that disagree — informing nobody. New **`js/mars-clock.js`** is therefore a **display, not a second time system**: it reads the one phase `environment.js` already advances and dresses it as Mars local time. **Noon *is* the sun's apex** (phase = π/2) by construction, so the card can never contradict the sky. The sol **number** is anchored to the real mission — the site's landing epoch gives the sol Perseverance is genuinely on today (**1918** at this commit) and each in-game sol you play ticks it forward by one: the site's real history plus your own. Placement is a **footer strip across the bottom of the minimap** — the only slot free on *both* viewports (the top bar can't take another chip at 390 px and every rail is spoken for), and it rides along when the map moves left in Wave 10. LOCK appears there while the cycle is frozen; the sun glyph goes cold-blue after dark. E2E 27/27: swept the phase through a full sol against the real sun vector — apex reads **12:xx**, nadir **00:xx**, every full-sun hour falls in 06–18 and every dark hour in 20–04; a quarter-sol advances 6 Mars hours; one full sol bumps the sol by exactly 1 and lands back on the same time of sol. |
| 03:05 | `fd4f5b5` | Mars Colony Wave 9.4 — **GPS wayfinding + relay antennas at every base** (#11). "Flying the drone I have no idea which way the rover or the base is." `waypoint.js` already pointed at exactly *one* thing (the nearest uncollected sample); new **`js/comms.js`** generalizes that maths to N targets — every other unit plus the nearest base, fed at telemetry rate. **Two readouts, two conventions, each matching its own frame of reference**: colour-coded ticks ride the **compass rim** at each target's *absolute* bearing (the dial is north-up, so a tick at the top means "north of you"), while the new **GPS RELAY card** shows *steer angles off the nose* (up = dead ahead), like the existing TGT arrow. Track colours deliberately avoid the needle's amber — the needle is your own nose and a tick must never be mistaken for it. **Antennas**: a relay mast rises at every base (lab at boot, each structure as it goes up) — the in-world answer to "how do the units know where each other are", and a silhouette on the skyline; procedural mast + dish + blinking navigation lamp, with a `models.js` `antenna` entry so the Tripo GLB swaps in when it lands. **No signal-range gate** — this is a wayfinding aid, not a difficulty layer. The card is desktop-only for now (phone rails are full: minimap → compass → gear → drone board) and the compass stays right-docked — moving it left today would land it on the mobile telemetry card, so it travels with the minimap in Wave 10.3. E2E 32/32: bearings checked against ground truth — a target due east reads **090**, due north **000**, due west **270**, ranges exact; `rel = abs − nose` to 0.6°; the active unit drops out of its own list on switch with no stale rows; mast sited 12 m off a new checkpost; mobile keeps the ticks, hides the card, rail still clear (compass ends 264 px, gear starts 272 px). |
| 02:35 | `516ce52` | Mars Colony Wave 9.3 — **base name plates + travel-to menu** (#6). Established bases were anonymous: three teal beacons on a 6 km map with no way to tell which was which, and no way back to one but to drive. Each structure now carries a canvas-textured billboard plate above its roof. A sprite is a fixed **world** size, so a plate legible from the 12 m chase cam is a speck from a drone 400 m up the ridge — the scale is stretched with camera range (clamped 0.85×–7×) to hold a near-constant on-screen size across the range you actually read it from (measured 2.6 m → 21 m tall between 17 m and 500 m out). New **BASES — TRAVEL** menu section lists every established base with its live range from the unit you are driving; the FIELD LAB heads it (it is a base too, it just arrived by cargo drop instead of being earned). TRAVEL puts the unit down on that base's **chargepad**, not on the structure — you arrive docked, charging, and repairing if it's the rover, which is the whole reward for having built the place (ties 9.3 back to 9.1/9.2). New `teleport()` on all three units: after Wave 9.9 a bare position write is no longer enough — the rover's gravity integrator holds an *absolute* body height, so it would arrive mid-fall from the old terrain, and the drone must keep its AGL (cruise in, cruise out) with any fall in progress caught. The BASES list rebuilds on the menu's closed→open **edge**, not per frame: a per-frame `replaceChildren` swaps the button out between mousedown and mouseup and the TRAVEL click never fires. E2E 24/24 — rover travelled 641 m to 0.00 m from the pad centre, docked, charge *and* hull climbing, not left mid-fall; drone at 13.5 m AGL arrived at 14.8 m. |
| 03:40 | `c7e776c` | Mars Colony Wave 9.9 — **Mars gravity (3.72 m/s², 0.38 g)**, the sim's first real vertical physics (#17). Until now NO gravity existed anywhere — not Earth's, not Mars': drone altitude was an input-driven accumulator (cut the power mid-air and it descended at a polite constant rate, rotors dead, like an elevator), and rover/humanoid snapped to terrain height every frame. New `physics.js` holds the one constant. **Drone:** losing power aloft is now a FALL — it accelerates at exactly 3.72 m/s² (measured), and restoring power catches it mid-air. **Humanoid:** SPACE jumps a genuine 0.38 g arc — apex 1.53 m, hang 1.79 s (theory: 1.50 m / 1.80 s); the same impulse on Earth would clear only 0.57 m, and that 2.6× floatiness is the most legible "not on Earth" cue in the game. Edge-triggered, so holding SPACE can't pogo. **Rover:** one vertical integrator covers both asks — drive off a fast drop-off and the ground falls away quicker than the wheels can follow (airborne, −8.6 m/s, hard landing bills the hull); hit a rising crest at speed and the body inherits the ground's upward velocity (airtime). A suspension tolerance keeps the wheels planted over shallow dips — without it the micro-relief had the rover "airborne" 12% of frames on the flattest ground. Note Jezero's DEM has no true cliff (steepest drop ~1.79 m over 3 m ≈ 31°), and a rover correctly does *not* fly down a 31° slope. E2E 15/15. |
| 02:05 | `e71d6ac` | Mars Colony Wave 9.2 — **rover hull damage**, a stat distinct from the battery (#7). Charge is spent and refilled; HULL only ever goes DOWN in the field and the one way back up is parking at a base. Three loss paths, mapped to what actually breaks a rover: **boulder strike** (edge-triggered on the impact frame — grinding against a rock you already touch must not bill every frame; priced on carried speed, ~14% at 6 m/s, and the hit kills 80% of the speed so it lands as a crunch), **pebble/washboard wear** (reuses the micro-relief term that already drives the suspension bounce; a rate-capped slow bleed — uncapped, single-frame relief spikes wrecked the rover in 30 s), and **rollover** (−14%). A battered hull is *slower* (limp mode, floor 0.45×) so damage costs something you feel, but 0% still drives — stranded-with-no-recourse is never fun. Repair happens only on a base chargepad. New HULL gauge in telemetry (rover-only; inverts the ROLL gauge's colour language since low is bad here). Constants tuned against measured terrain, not guessed: roughness means 0.14/peaks 0.5 at G2 vs spiking at G3. E2E 13/13. |
| 01:30 | `fc1e4c7` | Mars Colony Wave 9.1 — **solar chargepads at every base**, fixing the real bug behind "charging is stopped / the drone won't recharge at base" (#5). Charging was never regressed: ambient solar is gated on `env.daylight()`, which is **exactly 0 for ~37% of the sol**, and a landed unit has load=0 (no drain) *and* solar=0 (no recharge) — so a drone that flattened its battery after dusk sat at 0% with **no recovery path at all** until sunrise (~15 real min hard-stuck), the HUD giving no hint why. New `chargepad.js`: park ON a pad to charge fast and, crucially, **without the daylight gate** (the station runs its own battery bank) — that gate is the entire point of docking rather than just landing. Pads at the FIELD LAB from boot (the origin base, else there's nowhere to dock before the first outpost) and beside each structure as it rises (`outposts.js` gains an `onBuilt` hook; bootstrap re-derives them, no new save state). Docking requires being *settled* — hovering over a pad still burns rotors. HUD spells it out: "40% ⚡ DOCK" vs the "☀" ambient trickle. E2E 11/11; decisive pair at daylight=0: docked → charge RISES (5.00→5.21%), same drone off-pad → dead flat (20.0→20.0%). |
| 00:52 | `025762b` | Mars Colony Wave 8 (playtest follow-ups, bugs first): HQ minimap marker is now a haloed **star** instead of a slightly-bigger teal square — on a 140px map already crowded with teal squares (lab, checkposts), amber circles (samples) and orange diamonds (caches), a size-only cue was unreadable; verified on the live canvas (66 bright px at the HQ vs 0 at a checkpost, 3.2× lit-pixel density). In-world the HQ beacon deliberately keeps the checkposts' cyan (amber washed the white hull gold in Wave 7), so the split is minimap-only. HUD overlap fixes, both measured via `getBoundingClientRect`: desktop GEAR sat on the 231px-tall telemetry card (`bottom:215px` → `285px`); mobile GEAR landed squarely on the compass (46×30px hit) — right rail re-stacked minimap (70-212) → compass (218-264) → gear (272-302) → drone board (310+). Zero overlaps re-probed at 1440×900 and 390×844. Also diagnosed (no code change; feeds Wave 9): **drone charging is not regressed** — `daylight()` is exactly 0 for ~37% of the sol and a landed drone has load=0 *and* solar=0, so a drone that dies at night is stranded with no recovery path until sunrise (Wave 9.1 chargepad is the fix); and **no gravity is simulated anywhere** (not 9.8, not Mars' 3.72) — all vertical motion is scripted/kinematic (Wave 9.9 adds it). |
