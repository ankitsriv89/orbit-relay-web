# Issues & Resolutions

Running log of bugs and their resolutions. Newest day on top. One row per issue:
Issue | Root Cause | Resolution | Commit.

## 2026-07-28 (plan 33 wave 6 — the daily brief)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| `brief.test.mjs` seeded `DECAY_EPOCH` with `toISOString()`, so the reentry horizon filter and the day countdown were both being tested against a shape Space-Track never sends. **All 31 checks were green throughout** | `DECAY_EPOCH` is `varchar(24)` upstream, not a datetime — real values arrive as `2026-07-26 04:12:00`, space separated and with no zone (`modeldef_decay.json`, and `ingest.test.mjs` already used the right form). Found by reading the fixture against the schema, not by a failing test. This is the **same gap that let the GP `predicates/` bug through 151 green checks**: the data was real, but it was not data from the query under test | Fixture now generates Space-Track's format, and a check asserts it *is* that format so it cannot silently drift back to ISO and go green on a query that cannot work. Horizon comparison changed from a whole-string compare against an ISO instant — two different formats, correct only because `' '` sorts below `'T'` — to `substr(DECAY_EPOCH, 1, 10)` against a date, the same normalisation `ingest-decay.js` already applied | `3fd5fd8` |
| `days_until` was off by one for any host not on UTC | `daysUntil()` called bare `Date.parse()` on the zone-less epoch. V8 reads that as **local time**, and the error passes through a `ceil()`: `2026-08-01 12:00:00` is 4.0 days after the test's reference instant, but returned **5** under `TZ=America/Los_Angeles`. GitHub Actions runners are UTC so production was unaffected — `scripts/brief-preview.mjs` and any non-UTC runner were not | Separator and zone made explicit before parsing. The regression test pins an absolute instant and the suite is run under UTC, `Asia/Kolkata` and `America/Los_Angeles` | `3fd5fd8` |
| `/api/decay-watch` (wave 4) had the identical local-time parse | Same `Date.parse()` on the same field, written before the brief existed | Fixed rather than left, because wave 6 put DAILY BRIEF directly above DECAY WATCH in the same corner listing the same objects — two adjacent panels disagreeing by a day about a reentry is a visible contradiction. `parseEpochUTC()` moved into `_catalog.js` so there is one answer instead of two copies drifting | `1a52c8e` |
| The timezone fix shipped an hour earlier **nulled every countdown on `/api/decay-watch`** — 200 of 200 rows — and no test noticed | Space-Track sends `1957-12-01 0:00:00`: the **hour is not zero-padded**. The fix rewrote the string to `1957-12-01T0:00:00Z` and re-parsed it, and that is not valid ISO, so `Date.parse` returned NaN where the lenient parse it replaced had succeeded. A one-day error was traded for a total loss. **The same mistake as the entry below it, one layer down**: the fixtures were *generated* rather than captured — `toISOString().replace('T',' ')` yields `00:00:00` — so both sides of every assertion came out of one formatter and agreed with each other about a format production does not use | Components read with a regex and assembled via `Date.UTC`, rather than a string repaired well enough for a lenient parser. Test values are copied out of a live `/api/decay-watch` response, there is an end-to-end check that an unpadded hour reaches `days_until`, and the two copies of the parser (the ingest and Pages halves cannot import each other) are asserted to agree on every case. **Found by curling production immediately after deploying, not by the suite** | `25c9cd7a` |
| With countdowns restored, the live reentry watch opened with **SL-1 R/B, SPUTNIK 1 and SPUTNIK 2 at −25,076 days** | The `decay` table holds Space-Track's *historical* decay messages alongside its forward predictions, and the query had only an upper bound — so a list whose stated claim is "ranked by soonest predicted reentry" was ranking 1957. The new brief's `reentry_watch` had the identical hole and would have printed `reentry ~-25076d` on the DAILY BRIEF panel, making it a wave 6 bug as much as a wave 4 one | Both queries floor the window at today's UTC date — *today* rather than *now*, so an object due in a few hours does not vanish before it has actually come down. Sputnik 1 is now a fixture, in its real unpadded-hour form. Two hardcoded-2026 decay fixtures were made relative (against a date floor a fixed future date is a time bomb that passes until the clock overtakes it), and the already-decayed fixture's prediction was moved into the **future** so the floor cannot be what excludes it and that test keeps proving the `DECAY_DATE` exclusion it is named for. Live after the fix: STARLINK-1719 in 0d, STARLINK-1666 in 2d, LVM3 R/B in 3d — zero null, zero negative | `1bb5dba` |
| On a phone, `#results-hud` sat **exactly on top of** `#signal-hud`, and `#signal-hud`/`#conj-hud` overlapped by 10px — one chip wholly unreachable, another clipped. Shipped, and missed by the mobile audit that was written to catch precisely this | Two separate causes. (a) Both `#results-hud` and `#signal-hud` were given `bottom: 64px` in the mobile block; on a desktop they are in *different corners* (left vs right) and only collide once the phone rules make both full-width. (b) A collapsed chip is **62px** on mobile — a 44px toggle (the audit's own touch-target minimum) plus 8px padding top and bottom plus borders — but the offsets kept the 52px step written when the chip was ~40px. Growing the touch targets silently broke the audit's own "distinct offsets so none covers another's toggle" invariant, and the CSS comment asserting "~40px" had been wrong ever since | Stack rebuilt as 64 / 134 / 204 / 274 (70px step, 8px gap), measured rather than derived from the old comment. The E2E now reads all four `getBoundingClientRect()`s at 390×844 and fails on any overlap. **Nothing else could have caught it**: every rule involved was correct, present and applied — only the geometry was wrong. Same lesson as the `.refresh-btn` cascade finding: measure the element, not the stylesheet | `52a1413` |
| The new E2E brief gate threw `ReferenceError: arguments is not defined` and aborted the run after 108 green checks | `page.evaluate(expression, arg)` requires the expression to be a **function** when an argument is passed. A bare statement using `arguments[0]` — which reads naturally and looks like the no-argument form already used elsewhere in the file — has no `arguments` binding in Playwright's evaluation wrapper | All five call sites use `'(card) => __spacetrack.renderBrief(card)'`, matching the arrow-function form the rest of the suite already uses for parameterised evaluates | `659418b` |

## 2026-07-28 (mobile audit — orbit / space-track)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| The audit's top-priority finding (H1: "Cesium renders at full devicePixelRatio — 9× the pixels") was **false**, and acting on it as written would have degraded the page | The finding reasoned by analogy from the sibling Three.js projects, which do need an explicit `setPixelRatio(min(dpr, 2))` cap. Cesium has no such gap: `useBrowserRecommendedResolution` **defaults to `true`**, which explicitly ignores `devicePixelRatio`. Measured on an emulated DPR-3 phone, a 375×812 CSS canvas had a 375×812 drawing buffer — an effective ratio of 1.0. The recommended fix, `resolutionScale = 1/dpr`, would have cut the buffer to a *third* of CSS resolution on top of a ratio that was already 1.0 | Measured before fixing (one `page.evaluate` reading `canvas.width` vs `clientWidth`). Shipped the two things that are actually useful: **pin** `useBrowserRecommendedResolution = true` so a default change or a "crisper globe" edit cannot silently triple fragment cost, and `resolutionScale: 0.85` below 600px for the ~28% saving that genuinely was available. The audit doc is corrected in place rather than quietly marked done | see mobile commit |
| Safe-area insets applied as `max(px, env(...))` — the audit's recommendation — would have re-created the exact bug the panel layout was built to avoid | These HUD panels are collapsed title chips stacked at *deliberately distinct* offsets (74px / 140px in one corner) so that none can cover another's toggle. `max()` returns the larger of the two, so any inset exceeding the smaller offset collapses the gap between the chips and puts one panel's toggle back underneath another's body — an unreachable control | `calc(base + inset)` everywhere instead: it shifts the whole stack inward by the same amount, preserves the spacing between chips, and is a no-op at 0. The reasoning is recorded on the `--sa-*` declarations so the next person does not "simplify" it back to `max()` | see mobile commit |
| `.refresh-btn` stayed 26×26px after being explicitly given `width/height: 44px` in the mobile media block | Cascade order, not specificity. `.refresh-btn`, `.source-btn` and `.source-row__label` are declared in a "STANDALONE additions" section **after** the `@media (max-width: 600px)` block in `orbit.css`, so at equal specificity the later base rules win. `.source-btn` partly escaped only because its override used `min-height`, a property the base rule never sets — which made the failure look inconsistent rather than systematic | Mobile sizing for those components moved to a trailing media block placed after the rules it overrides, with a comment explaining why it cannot be hoisted. Caught because the verification measured `getBoundingClientRect()` on the real element rather than asserting the rule existed | see mobile commit |
| `requestRenderMode` can freeze the globe completely while every other test still passes | With render-on-demand, nothing draws until something calls `requestRender()`. Positions still update, `tickCount` still climbs, worker agreement still holds, `scene.pick()` still resolves — every existing assertion in the suite reads JS state, not pixels. A missed call produces a still image and a green test run | `SatEngine.requestRender()` is called from all six paths that change the scene (worker positions, sync fallback, pulse, add/remove satellite, async orbit-ring placement), and `mobile_gate()` counts `postRender` events over 4 s on both pages, failing at zero. The rule is written on the method itself: every scene-changing path calls it | see mobile commit |

## 2026-07-28 (plan 33 wave 5 — derived conjunction screening)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| The first real browser run reported a close approach as happening **`in 14m`** for a pair whose relative velocity was 6.5 m/s — a time of closest approach that was pure noise | The two objects were co-orbiting, so their separation is *flat* across the whole window. The TCA solve minimises `\|Δr + Δv·t\|`, which for a near-zero `Δv` is a nearly flat parabola: the reported minimum is wherever floating-point rounding put it, not a physical event. Every number in the row was correct; the *framing* claimed an event where there was a standing formation. No test caught it because the unit suite asserts miss distance and TCA against closed-form crossings, where `Δv` is 15 km/s and the minimum is sharp | Pairs with a relative speed under **100 m/s** (two orders below a genuine crossing, comfortably above the drift inside one launch's debris cloud) are labelled **`co-orbiting`** instead of being given a time. Naming the formation is truer *and* more useful — it usually means one launch or one orbital plane. E2E now asserts the label on exactly this geometry | see wave 5 commit |
| The TCA solve could report a close approach that never happens, at a time possibly outside the screening window | `τ = −(Δr·Δv)/\|Δv\|²` blows up as `\|Δv\| → 0`. A near-co-orbiting pair — two objects in one constellation plane, or one launch's debris cloud — has a nearly flat separation parabola, so the unconstrained minimum can land thousands of seconds from the sample. The straight-line model is only valid across the ±Δt bracket where relative acceleration is negligible; extrapolated that far the orbits have curved and `\|Δr + Δv·t\|` is fiction. Noticed from a 23 mm worker-vs-main-thread discrepancy on a co-orbiting pair that should have agreed exactly | τ is clamped to ±one step **and the reported miss is evaluated at the clamped τ** (`missAtTau()`), so every number is backed by a propagation the screen actually performed. Costs nothing on a real crossing (τ is a fraction of a step there) and completeness is unaffected — the march visits every step regardless. The test asserts the invariant, not a constant: for every row, the reported miss must equal what an independent propagation finds at the reported TCA | see wave 5 commit |
| The wave 5 E2E passed while silently testing a corrupted element set | `twin_tle()` formatted a 6-digit catalog number (`900001`) into line 2's **5-wide** field. A TLE is fixed-column, so the line grew by one character and every field after it shifted: satellite.js read the twin's inclination and mean motion off by a column. Nothing failed, because the independent comparison propagated the *same* corrupted line — the agreement assertion was still satisfied. A test passing for the wrong reason | `twin_tle()` now asserts the catalog number fits 5 columns, the mean-anomaly field is exactly 8 characters, and the resulting line length is unchanged. Any future splice that shifts a column fails loudly instead of quietly changing the orbit under test | see wave 5 commit |
| A screening run that was superseded by a newer one re-enabled the RUN button and hid the progress bar, so the panel read as idle while the worker was still going | `screener.run()` settles the previous promise with `superseded`, and the page's `catch` called `endScreenProgress()` *before* testing which error it had — so the losing run tore down UI that the winning run owned | The `superseded` branch returns before touching any control: a newer run owns the panel. Found by reading the path rather than by a failing test — the RUN button is disabled during a run, so only a programmatic second call (the debug handle, or a future auto-rescreen) can reach it | see wave 5 commit |

## 2026-07-27 (plan 33 — first live seed: GP predicates trap + a duplicate dispatch)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Every one of the 31,629 seeded objects landed with `TLE_LINE1`/`TLE_LINE2` **NULL**, silently breaking all 20 group bundles and the entire `source=spacetrack` `/api/tle` path | A `predicates/` projection on `class/gp` with `format/json` — even one that explicitly lists `TLE_LINE0,TLE_LINE1,TLE_LINE2` — comes back with those three fields simply **absent**, not a 400 and not empty strings. `TLE_LINE1`/`2` are synthesized by Space-Track from the numeric elements rather than stored columns, so being listed in `modeldef/class/gp` does not guarantee they survive a narrowed projection. Every unit suite passed throughout because `fixtures/sample_gp.json` was captured as a FULL, unfiltered response (it has TLE lines) — nothing in the suite exercised the narrowed shape actually sent in production | `Q.gpDelta`/`Q.gpFull` no longer build a `predicates/` segment at all; GP always requests the full record. A regression test calls `Q.gpDelta(GP_PREDICATES)` directly and asserts the argument is ignored. No data was lost — `TLE_LINE1`/`2` backfill via the normal idempotent upsert on the next real GP ingest | `3b6c14b` |
| Two real full-catalog GP calls hit production Space-Track ~26 minutes apart in the same hour, plus a third run queued behind them | Two `workflow_dispatch` runs of the `seed` job existed almost simultaneously (18:27:15 and 18:29:23 UTC) though the CLI (`gh workflow run … -f job=seed`) was issued only once — most likely a second trigger via the GitHub Actions **Run workflow** button around the same time, plus the `17 */6 * * *` schedule became eligible once secrets went live mid-session and queued a third run behind the `concurrency: orbit-ingest` group | Caught by checking `gh run list` before assuming the seed was the only activity. Cancelled the in-flight duplicate (`gh run cancel`) before its "Build the R2 artifacts" step could make further satcat/decay/boxscore calls, and cancelled the queued scheduled run before it could start. Both completed GP calls returned 200 — no rejection observed — but the incident is why **the same job should never be dispatched from two places without checking `gh run list` first** | n/a (process, not code) |

## 2026-07-27 (plan 33 waves 2b + 3 — Actions ingest, shared engine, /spacetrack/)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| The ingest could never have run on the deployed Worker at all | The GP job parses and upserts most of the ~28k catalog in one invocation: ~300 ms of CPU against **Workers Free's 10 ms per invocation**. Streaming solved the memory ceiling and was mistaken for solving the whole problem. Cadence does not help either — CPU cost is driven by rows/day, which is fixed, so even 1-hourly (the fastest GP rate Space-Track permits) is ~55 ms, still 5× over | The runner moved to GitHub Actions, which has no CPU ceiling. Not a rewrite: `scripts/env-node.mjs` reimplements the narrow binding surface `src/*` was already written against (`prepare().bind().run()/all()/first()`, `batch()`, R2 `put()/get()`) over the D1 HTTP API + R2 SigV4, with `ORBIT_KV` as a Map. Every ingest module is untouched, so the whole existing suite still covers the code that runs in production | `73e7681` |
| Routing D1 through the REST API would have silently re-broken every numeric filter — the *same* bug wave 2 had just fixed | The D1 REST API types its `params` array as **string**. `derive.js` coerces Space-Track's string scalars to numbers precisely so `WHERE PERIOD BETWEEN 1430 AND 1450` compares numbers; passing those numbers back through a string-typed parameter array would have undone the coercion at the last hop, and invisibly, because the symptom is wrong rows rather than an error | Parameters are inlined as SQL **literals** instead, with quote-aware placeholder substitution and an arity check that throws rather than producing SQL that happens to parse. `test/env-node.test.mjs` runs the generated SQL through a real `node:sqlite` and asserts `typeof(PERIOD)` is `real`. Side benefit: `batch()` becomes one multi-statement request, so a full delta is ~700 round trips instead of ~28,000 | `73e7681` |
| A hand-rolled SigV4 signer is trivially self-consistent and still wrong | Testing a signer against its own output proves only that it is deterministic. There was no independent oracle in the repo, and the AWS docs example could not be retrieved verbatim (a web fetch produced a *fabricated* signature, which is worse than none) | Reference signatures generated locally with **botocore** for a fixed request and pasted in as vectors, so the test fails if the two implementations ever diverge. Also asserted that the citation string is ASCII: it ships as an S3 metadata header, where a curly quote or em dash throws inside `fetch()` and would take the whole 17:35 artifact rebuild with it | `73e7681` |
| `/spacetrack/` would have run SGP4 on the main thread, with nothing reporting a problem | `new Worker('worker/propagate.worker.js')` resolves **against the page**, not the module. Shared by two pages at different depths, `/spacetrack/` would have requested `/spacetrack/worker/propagate.worker.js`, got a 404, and taken the deliberate synchronous fallback — which is a performance cliff (82.8 ms vs 0.40 ms per tick) that logs a warning and otherwise looks fine | The engine takes an **absolute** `workerUrl` defaulting to `/orbit-engine/propagate.worker.js`, and `importScripts` inside the worker is absolute too. The E2E asserts the worker is live *on the new page* specifically, with the failure message naming this cause | `e51378d` |
| `/api/object/25544;%20DROP%20TABLE%20objects` answered **200** with a real dossier | `Number.parseInt` stops at the first non-digit and returns 25544, which passed the `Number.isInteger` guard. Never an injection — the value is bound — but a malformed path that returns a plausible record hides the caller's bug instead of reporting it | The whole path segment must match `/^\d+$/` before it is parsed. Found by `test/pages-api.test.mjs`, which calls the Pages Functions' `onRequest` against a seeded `node:sqlite`; the E2E could not have found it, because the static test server has no Pages Functions at all | `f2d845a` |
| Converting `orbital-relay.js` to an ES module turned a dead variable into a hard crash | `inspectedSatrec` was assigned and never read. As a classic script that silently created a global; under a module's implicit strict mode the same assignment is a `ReferenceError`, and it took out click-to-inspect | Removed the variable and its assignment. Caught immediately by the existing E2E — worth noting as the general risk in that conversion: sloppy-mode globals are exactly what module conversion turns fatal | `e51378d` |

## 2026-07-27 (plan 33 wave 2 — Space-Track ingest)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| The GP ingest as designed would have run a Worker out of memory on its first real run | The plan sized the 6-hourly delta as "a few hundred to ~2k objects". It is not: `gp` returns the *latest* elset per object and 18 SPCS regenerates elsets continuously, so a 6.7-hour `CREATION_DATE` window matches **most of the ~28k active catalog** — a 10–20 MB response. `await resp.json()` holds the response text *and* ~28k parsed objects simultaneously, past the 128 MB Worker limit | New `src/jsonstream.js`: a scanner that tracks string/escape state and brace depth to find each top-level element's boundaries and hands that slice to the real `JSON.parse`. Only the *framing* is ours — the part doable in one pass without buffering. Resident set is one row plus the 40-row upsert batch, independent of catalog size. Asserted by parsing the fixture one byte at a time (identical to `JSON.parse`) and by proving the generator yields a row while the stream is still open | `2d51d57` |
| The GEO group would have been silently empty, and every numeric filter with it | Space-Track returns **every scalar as a JSON string** (`"PERIOD": "132.594"`). D1 stores what it is given, so `WHERE PERIOD BETWEEN 1430 AND 1450` becomes a string comparison — `'132.594'` sorts after `'1430'` — and matches nothing. Nothing errors; the group file just comes out empty | Explicit numeric coercion at ingest via a `NUMERIC` column set in `derive.js`. Guarded in `test/sqlite.test.mjs` by asserting `typeof(PERIOD) = 'real'` and `typeof(NORAD_CAT_ID) = 'integer'` **in a real SQLite**, plus a range query that actually returns its row | `2d51d57` |
| Switching to SPACE-TRACK would have shown Celestrak satellites under a "Live data: Space-Track" label | The source button called `reloadAllLayers()`, which iterates `.layer-cb` checkboxes and skips `data-builtin="true"`. The ISS, the other stations and the Starlink batch are built once by `loadSatellites()` at boot and are not in `layerState` at all, so the majority of visible points were never re-sourced. Invisible while Space-Track was disabled | The switch now calls `location.reload()`; `activeSource` is read back from `localStorage` at boot, so the choice survives. E2E asserts the page comes back up on the new source *and* that the boot fetches carry `source=spacetrack` | `2d51d57` |
| The source-switch E2E crashed with "Execution context was destroyed" | `location.reload()` is triggered from inside the click handler, so the `page.evaluate` that dispatched the click was still in flight when the navigation tore down its context — and `wait_for_load_state('load')` could return for the *pre-reload* document | `switch_source()` uses `page.dispatch_event` (which also sidesteps the actionability gate that makes `page.click()` time out on this box) and then polls for `window.__orbit`, swallowing the exceptions thrown during the navigation window. It waits on `__orbit` existing, **not** on `booted` — under Space-Track the static test server has no `/api/tle`, so zero satellites is the correct outcome there | `2d51d57` |
| `Q.gpDelta()` asked for all 40 GP fields including seven that are identical on every row | The OMM envelope metadata (`CCSDS_OMM_VERS`, `COMMENT`, `ORIGINATOR`, `CENTER_NAME`, `REF_FRAME`, `TIME_SYSTEM`, `MEAN_ELEMENT_THEORY`) is constant, and the plan listed a `predicates/` projection as a call-reduction measure that had not been wired | `GP_PREDICATES` is **derived from `OBJECT_COLUMNS`** rather than typed out, so it cannot drift from the schema it fills — a predicate name that does not exist upstream is a hard 400 on every ingest. Tested against `modeldef/class/gp` both ways: nothing requested that upstream lacks, nothing stored that is not requested | `2d51d57` |

## 2026-07-26 (plan 33 waves 0-1 — Orbital Relay)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| The standalone `/orbit/` still carried the memory design that blew the parent's heap to 1.2 GB (marsapiens issue **#71**), so adding Space-Track's ~28k objects on top would only have fed it faster | The June 2026 sync copied the enhanced Orbital Relay **into** marsapiens, and the `PointPrimitiveCollection` fix was then made *there only* — it never came back the other way. The standalone kept one Cesium Entity per satellite with a `CallbackProperty` position, which re-runs SGP4 for every sat every frame (~36k propagations/sec at 600 sats × 60fps) on top of the per-Entity property-bag overhead | Ported the engine: all sats are PointPrimitives in one collection, positions written in place by a throttled tick off the Cesium clock. Guarded by an E2E assertion that sat count and entity count **differ** — the shape of the regression, not a proxy for it | `92aadec` |
| Iridium always took the slow API path and never used its shipped baseline snapshot | `fetchTLE` lowercases the group slug to build the baseline URL, but the Celestrak group name is mixed-case (`iridium-NEXT`) and `snapshot_tle.sh` wrote the file under the group name verbatim. The 404 was silent because the API fallback succeeded | Renamed the file to lowercase **and** fixed the generator, which would otherwise have reintroduced it on the next snapshot refresh. E2E now fetches the baseline for every group in the layer list | `92aadec`, `9d84e8a` |
| The wave-1 E2E reported **23.7 km** of drift between worker and main-thread positions — indistinguishable from a coordinate-conversion bug in the new geodetic→ECEF code | Sampling error, not math. WebGL on this box is SwiftShader at ~0.3 fps, so the main thread can go **seconds** without running the `message` handler that applies worker positions. A fixed `time.sleep(3)` therefore compared a several-second-stale coordinate against a freshly propagated one, and the ISS covers 7.66 km/s | The page exposes a `tickCount`; `wait_ticks()` blocks until the worker has completed N more ticks, so comparisons happen on coordinates from the same instant. Real drift is **0.10–0.20 m**, which is exactly Float32 quantisation at Earth's radius. Recorded in the test docstring — fixed sleeps are the wrong instrument on this box | `9d84e8a` |

## 2026-07-26 (six new Mars sites)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| The three sites pinned as "COMING SOON" on the hub globe (Hellas, Olympus Mons, Meridiani) could not be built the way Jezero and Gale were | **No high-resolution elevation data exists for any of them.** An audit of the whole `asc-pds-services` mirror found the best global DEM is `Mars_HRSC_MOLA_BlendDEM_Global_200mp_v2.tif` at **200 m/px** — 10× coarser than Jezero's 20m DTM. At a 6km worldSize that is **30 elevation samples across**: the clipmap and physics would be sampling an almost featureless surface | Split the roster into two tiers. **Traverse sites** (6km, 20m DTM) came from a *different* discovery — `mars2020_landing_site_dtm/` and `insight_landing_site_dtm/` carry co-registered **20m DTM + 6m ORTHO** pairs, byte-for-byte the Jezero recipe, at Gusev, NE Syrtis and Elysium. **Regional sites** (30–90km) use the 200m DEM at a scale where it has 150–450 samples, paired with the **`Mars_MRO_CTX_Equi_Mosaics_Robbins/6mpp/`** quads so texture stays Jezero-grade even where geometry cannot. All six shipped rather than three | `802b041` … `f9dbb21` |
| Gusev's first bake had only **78m** of relief and no Columbia Hills — the whole point of Spirit's mission | The AOI was centred on Spirit's *landing site*. Columbia Hills lies ~4.7km ESE, so a 6km box centred on the landing put the massif ~0.01° outside the east edge | Re-centred on the **midpoint of the mission arc** rather than the landing: Columbia Memorial Station lands at world (−1916,−1323) and Husband Hill at (+1916,+1323), holding both ends of the real drive, relief up to **114m**. Hill positions came from **scanning the DTM for local maxima**, not published coordinates — and the resulting frame reproduces Spirit's published landing coordinate exactly (HUD: 14.56919 S, 175.47291 E), which is the actual check that the projection chain is correct | `802b041` |
| Every AOI centred on InSight's lander came back **100% nodata**, despite the lander being inside the DTM's bounding box | **Stereo DTM footprints are angled parallelograms, not filled rectangles.** Five of the seven products in `insight_landing_site_dtm/` have their bounding box over the lander with no data at it — `gdalinfo` extents are the *envelope*, and say nothing about coverage at a given pixel | Probed all seven with `gdallocationinfo` at the lander coordinate; `F02_036761_1828_F04_037262_1841` returns **−2613.95m**, matching InSight's published elevation. The running game now reports ground **−2613.65m** under the spawned rover. Rule recorded in `prep_site.sh`: probe the actual pixel, never trust the bounding box | `90794a7` |
| Olympus Mons' caldera kept landing off-frame — first in the SE corner, clipped | Two bad centre estimates in a row. The textbook coordinate (133.8W, 18.65N) is ~50km off the caldera centroid at this crop size; and taking the **DEM's global maximum** as "the summit" landed on a flank high at (133.06W, 17.31N), about **90km** away — the highest point is on the rim/flank, not at the caldera | Rendered the DEM's relief to PNG and **measured the depression directly**: centre (133.1678W, 18.3271N), complex ~78km across. Framed at **90km** so the whole nested complex sits inside its rim. General lesson recorded in the plan doc: for a landform this large, image the DEM — neither published coordinates nor `argmax` locate it | `a4d7e7b` |
| Hellas could not use its own hub pin location at all | The locked pin sat at (70E, 42.4S), where there is **no CTX coverage**. The Hellas exploration-zone mosaics cover 99.66–106.28E / 37.32–41.81S — a completely different part of the basin | Relocated the site onto the real imagery: 30km at (102.97E, 39.56S) on the eastern rim, with ortho data verified at all four corners **and** the centre via `gdallocationinfo` before baking. Also the only site that crosses the datum (−694m to +2574m) | `f9dbb21` |
| Meridiani would have rendered **two overlapping pins** on the hub globe | `hub.js` builds its pin array as `[...Object.values(SITES), ...LOCKED_SITES]`. Promoting a site to playable without removing its `LOCKED_SITES` entry therefore yields two pins at identical coordinates — one playable, one greyed "COMING SOON". Silent, and only visible by spinning the globe to that exact spot | Removed the entry, documented the invariant at both list definitions, and added an E2E check (`no site is both playable and locked on the hub globe`) that reads `SITES`/`LOCKED_SITES` off a new `__mc` handle. `LOCKED_SITES` is now empty — all 8 sites are playable | `aebfce3` |
| Hub globe labels were cut mid-word — "Olympus Mons" drew as "Olympus Mor" | Pre-existing, and nothing to do with canvas width. A label sprite is centred on its pin, so for a pin near the limb the far half of the sprite is geometrically *behind* the globe surface, and `depthTest: true` on the sprite material let the planet occlude it. Invisible until a site landed under the default camera rotation with a name long enough to reach the horizon | Disabled `depthTest` on the label material. Nothing is lost: labels for pins over the horizon are **already** culled explicitly by the `isFront()` test at `hub.js:586`, so depth testing was only re-doing that job, badly, for the pins it could actually see | `a96e23f` |
| An unconditional 16-bit→Byte conversion would have silently changed Jezero's and Gale's albedo | The CTX Robbins quads are 16-bit and JPEG output must be Byte, so the regional sites need `-ot Byte -scale`. But `-scale` with no arguments auto-stretches source min/max — applied to the existing 8-bit mission orthos it would contrast-stretch them into different-looking terrain than currently ships | Made it opt-in per site (`ORTHO_SCALE=1`), with the reasoning written at the variable so it is not "tidied" into an unconditional flag later. Same treatment for per-source AOI bounds (`DEM_ULX/…`), needed because the blended DEM is in **degrees** while the Robbins quads are Equirectangular Mars **metres** (`lon_0 = −180`) | `aebfce3` |

## 2026-07-25 (Gale site parity + Gratbot dance move 2)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| On **Gale**, the mandatory first mission step ("REPORT TO ARIANA AT THE FIELD LAB FOR BRIEFING") greeted the player with *"Welcome to **Jezero** Crater … **Perseverance** … the **Séítah** dune field, the **Neretva Vallis** channel"* — the wrong site's narrative, on the one step you cannot skip | `hologram.js` held Ariana's script as a module-level `SCRIPT_LINES` const with Jezero's geography written into it. Harmless while she was optional set-dressing (Wave 9.5); plan 28 promoted her to the **autostart tutorial's step 0**, which turned a cosmetic inaccuracy into the first thing Gale players read. Plan 21-B had synced Gale's *data* (`surveyZones`, `photoSpots`, `hazards`, `hq`, checkposts, buried core) but not this, because the text lives **outside** `sites.js` — an audit of every `site.*` field the engine reads confirmed all of them were already populated on both sites, so this string was the last Jezero-only content in the game | Split the script by **what varies**: the site-specific half moved into a new per-site `sites.js` **`briefing`** field (2 lines, each site's second line naming **that site's three `surveyZones`**, so the briefing previews the objectives); Ariana's two **character** lines (the ARIANA/ATHENA name story, "bring samples to the lab") stay shared in `hologram.js` as `ARIANA_LINES` — they are genuinely site-agnostic, so copying them per site would just create two things to keep in sync. `createLabHologram(scene, labPos, site)` composes `site.briefing + ARIANA_LINES`; a site without `briefing` still yields a valid script, matching the no-op-when-absent pattern the rest of the per-site config uses. Gale's lines carry real Curiosity/MSL science (lakebed, Mount Sharp, Bagnold, Peace Vallis, Vera Rubin) | `7c57a6a` |
| Ariana's briefing ran off **both** edges of the screen — the line rendered as "ana: Welcome to Gale Crater…" with the head and tail clipped | Pre-existing, not introduced by the briefing work. `.mars-hud__objective` was `white-space: nowrap` with **no `max-width`**, centered via `translateX(-50%)`, so any line wider than the viewport overflowed symmetrically. Only the mobile breakpoint (`max-width: 899px`, `pointer: coarse`) ever set `max-width`/wrapping. Measured at 1280px: Jezero's longest line **1240.6px** (fit, but with 40px to spare — it was already clipped on any desktop window under ~1280px), Gale's new first line **1309.9px** (overflowed). So the banner had been silently broken for narrow desktop windows on Jezero too | Shortened Gale's line into Jezero's length envelope, **and** fixed the container: promoted the mobile breakpoint's own idiom (`max-width: min(92vw, 1180px)`, `white-space: normal`, `text-align: center`) to apply at **every** width — preserving the existing design pattern rather than inventing a new one — and removed the now-redundant mobile overrides, leaving that media query to set font-size only. `flex-shrink: 0` + `white-space: nowrap` on `.mars-objective__skip` so wrapping text can't push SKIP onto a second line. Short objective steps still render as a single line; only long dialogue wraps. Verified in-viewport at **1920/1440/1280/1100/960/375** on both sites, and guarded by a new E2E check so a future site can't reintroduce it by writing one long line | `7c57a6a` |
| `tests/e2e/test_plan28.py` crashed on every run — the entire suite was unrunnable, and had been since the commit that added the check | The chassis-corner probe added in `b2734d8` was written `page.evaluate('((SHELL) => { … })()', SHELL)`. The expression **self-invokes with no argument**, so Playwright's second parameter is never bound and `SHELL` is `undefined` → `TypeError: SHELL is not iterable`, thrown before any dance assertion executed. Playwright only passes its arg when the expression evaluates **to a function**, not to that function's result | Dropped the trailing `()` so the expression is a function Playwright can call with the arg. Suite runs: **41/42**. The restored run then showed that `b2734d8`'s headline claim was **never verified in-browser** — its `TWO-LEG pose is heading- and framerate-independent` assertion **fails at a 1.533° pitch spread** (wants < 0.5°). Cosmetic and left open: the rear-up still lands in the 35–60° band (35.2°), chassis clears by **+0.355 m**, and no foot goes under the surface | `7c57a6a` |
| The new Gale parity suite reported ~88 phantom `THREE.GLTFLoader: Couldn't load texture blob:…` console errors, which looked like a Gale asset bug | Test-harness artifact, not a product bug. The suite originally visited both sites by re-navigating **one page**; navigating away revokes the first document's GLTFLoader blob URLs while the second document's loaders are still resolving them. Isolated with a probe: **0** blob errors with a fresh context per site, **88** when the page is reused | Each site now runs in its **own browser context** (`visit()`), which also isolates the per-site `mc-<siteId>-*` localStorage save state between the two runs. Rationale recorded in the function's docstring so the contexts aren't "simplified" back into one page | `7c57a6a` |
| Gratbot's dance move **2** (TWO-LEG) sank the robot into the ground nose-first. Persisted after the first fix attempt (`daa6792`), which had raised the body and reversed the pitch | **Two compounding bugs.** (1) `poseDance` built its rotation with `setFromAxisAngle(_right, …)` / `(_fwd, …)`, but those are **world**-space vectors while the quaternion is post-multiplied onto `mesh.quaternion` — a post-multiply applies its axis in the **body-local** frame. The two frames coincide only at heading 0, so the "nose-up" pitch **inverted to nose-DOWN at heading π** and degenerated into a **roll at ±π/2**. The earlier fix was verified at spawn heading, where it genuinely worked. (2) `mesh.quaternion` is *also* the terrain-tilt smoothing state (`slerp(_tiltQ, 1−e^(−10dt))`), so composing the dance offset into it fed the offset back into its own smoothing: steady-state error ≈ `d/k`, i.e. **framerate-dependent runaway**. Measured nose-up: **31.5° intended → 54° at dt 0.1, 85° at 0.05, 88.9° at 0.016 (real 60 fps)**, which buried the entire chassis (feet to **0.96 m below** the surface) at *every* heading. The E2E only sampled the body **origin**, which rises `+0.30` by construction and cannot see a nose-dive, at one dt and one heading — so it passed throughout | Split the smoothing state out into **`_baseQ`**: `update()` slerps `_baseQ` and copies it to `mesh.quaternion`; `poseDance` writes `mesh.quaternion = _baseQ · _danceQ`, so the pose is an **output, never an accumulator**. Dance rotations now use constant **body-local axes** (`_bodyX/Y/Z`), making them heading-proof. Rebuilt TWO-LEG as a true rear-up **pivoting about the hind-foot line** instead of the body centre: rise `hindLz·sin θ` and lean `hindLz·(1−cos θ)` toward the rear are *derived* from θ, not tuned — and because hip↔own-foot distance is a rotation invariant (0.68 m vs 0.88 m reach), the hind legs are always solvable at any rear angle and their plant never moves. Implemented in its general form — seat the body so the hind-foot line lands on the ground under the **full** pose, `position = P_world − q·L` — because deriving the rise from the dance pitch alone ignored the terrain tilt already in `q` and swung the rear corner **0.23 m into a 20° hillside**. `REAR_PITCH = 0.6` (~38° nose-up): deliberately moderate, since the terrain-hug tilt stacks on top and rearing swings the rear-bottom chassis corner down *and* backward, into ground rising behind the pivot. The seat shift is recorded in `_danceOfs` and **undone at the top of `update()`** — `mesh.position` is the step integrator's state, so leaving it applied drifts the walker backwards every frame. Added a `danceRamp` ease-in so the rear-up and the paw lift-off no longer pop in one frame. Verified headless 12/12 flat (**5 moves × 4 framerates × 8 headings**: min chassis **+0.375 m** AGL, no foot under the surface, pitch spread **0.0000°**, drift **0.0 m**/400 frames) and 8/8 on **20° grades in 4 directions × 3 dt × 8 headings** (min chassis **+0.089 m**) | *(local)* |
| The bug survived a fix + an E2E that both "passed" | The assertion measured the body **origin** (`+0.30 m` by construction) rather than the chassis **shell**, at a single heading and a single `dt` — blind to exactly the two axes along which this bug varied | `test_plan28.py` now sweeps **heading × framerate** and samples the 7 chassis shell corners from `gratbot.js`, plus asserts pose invariance across dt/heading and zero dance drift (2 checks → 6) | *(local)* |

## 2026-07-24 (humanoid leg IK + van speed)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Humanoid's legs sat folded up **over its back**, so the figure read as having no lower half ("invisible") — visible whenever it stood still | `humanoid.js` `solveIk` derived the hip angle as `atan2(dz,dy) − atan2(b·sin γ, a + b·cos γ)`. That second term is **wrong at extension**: with `FEMUR(0.208) < TIBIA(0.259)`, `a + b·cos γ` goes negative as the knee straightens (γ→π), so the term snaps to ≈**π** and flips the whole thigh 180°. A *standing* leg is at full extension (hip→ground 0.458 ≈ leg length 0.467), so the flip fired on essentially every idle frame. Measured: offset 68.9°→180° as r went 0.400→0.467, where the correct angle goes 35.1°→0° | Replaced the solve with a **world-space, bind-relative** two-bone IK: solve femur/tibia *directions* (correct law-of-cosines `β = acos((r²+a²−b²)/(2ra))`, knee poled toward body-forward), then re-aim each bone by the minimal rotation from its **bind** direction — no assumption about bind frame orientation. This also fixes two further latent errors the old code had: the angles were computed in **Pelvis-local** space but applied about each **bone's local X** (L_Thigh's bind is a ~180° X-flip → mis-signed), and the bind knee is already pre-bent ~0.335 rad (→ constant offset). Added `ANKLE_H` so plant points (soles) drive the ankle bone without sinking the boots. Verified 15/16 headless: hip>knee>ankle, feet 0.09 m above ground, planted <0.27 m from body centre | `9eb4015` |
| The same symptom was previously logged (2026-07-20, `a302ba0`) as *"stale deploy/cache, resolved by redeploy + hard-refresh"* | Local check looked at a rigged GLB with legs *present* in the scene graph and concluded the geometry was fine; the bones were there, but the **pose** was flipped, which a presence check can't see | Root-caused from the GLB's actual bind transforms (bone lengths/rotations read straight out of the glTF JSON chunk) plus a numeric FK check, rather than a visual presence assertion | `9eb4015` |
| Van felt too slow to drive | `VAN_SPEED = 2.6` m/s (real NASA SEV ~10 km/h) is only ~1.8× the 1.4 m/s walk and *below* the rover's slowest usable gear (G1 = 2.1 m/s), so driving out was slower than walking there | `VAN_SPEED = 5.5` m/s (~20 km/h) — time-compressed in the same spirit as the rover's `GEARS`, ~4× walking pace, still under rover G2 (6.3 m/s) | `9eb4015` |
| Legs **crossed** (left boot planting on the body's right) once the IK started resolving correctly | Pre-existing, and masked for as long as the legs were folded behind the back. Units FACE `-_fwd` — forward input is negative throttle (`main.js` W ⇒ `-1`) and `models.js` yaws every model's face onto local `-Z` to match — so body-right is `-_right`. The GLB's +90° yaw puts `L_Thigh` on group-local `-X`, i.e. world `+_right`, but `solveLeg` was planting the left boot at `-FOOT_SPACING × _right` | Named `L_LATERAL`/`R_LATERAL` constants carrying the derivation, so the non-obvious sign can't be "tidied" back. Verified 0/120 walking frames crossed, boots 0.245 m apart | `a1e97d2` |
| Knees hinged **backward** | My new knee pole aimed at `+_fwd`, but the unit faces `-_fwd` (same convention error) | Pole negated. 0/240 joint-frames backward (was 11/240) | `a1e97d2` |
| Legs jerked hard twice per stride while walking | `ikActive` was gated on `grounded`, so every Apollo-lope hop snapped both legs to **bind pose** for its airborne frames and back to the solved pose on touchdown. 40 of 140 frames are airborne at walking speed, so this fired constantly | IK stays live in flight; swing targets lift by `airY` so the legs hold their pose relative to the hips. Only a real jump (`airY > FOOT_RELEASE_AIR` 0.25 m) releases the boots — a lope hop apex is ~6 cm, and holding contact through push-off is also the truer read | `a1e97d2` |
| Boot teleported ~0.5 m at every stance handoff during the lope | `STEP_HALF` (0.35 m) was tuned for the un-loped cadence. The lope damps stride rate 40% while speed stays 1.4 m/s, so the body covers **1.22 m** per half-cycle against a **0.7 m** swing arc — the gap had to be jumped. `plantFoot` also recomputed a fresh lead off the body instead of using the boot's real position | Half-reach derived live (`stepHalfNow()`): the arc spans exactly the ground covered per half-cycle. Speed cancels out, so it only widens with the lope — which is also what a bounding lope looks like. Plus `plantFoot` now pins the boot at its last commanded position (the walker-rig continuity idiom). Max per-frame foot delta **0.95 m → 0.18 m** | `a1e97d2` |
| Swing lift was **inverted** — boot highest at touchdown, scraping the ground mid-step | `swingT=(sin(s+phase)+1)/2` fed into `sin(swingT·π)` peaks at `s=0`/`s=π` (the ground-contact transitions) and is exactly 0 mid-swing. The *identical* bug was found and fixed in `walker-rig.js` on 2026-07-20 but never back-ported to the humanoid it was copied from | Progress `u` running 0→1 across the leg's swing half-cycle, `lift = SWING_LIFT·sin(u·π)`. Median foot delta 0.076 m → 0.043 m | `a1e97d2` |
| Recon survey cone pointed straight down (nadir), hidden under the fuselage and reading as un-steerable | `updateSensorCone` put the cone apex and its ground footprint at the same `x/z` — a 90° nadir camera | Boresight tilts 35° forward along the drone's heading: footprint leads `agl·tan(35°)` ahead (14 m at 20 m AGL) and sweeps as you yaw. Ring stretched `1/cos(tilt)` down-range since the oblique ground cut is an ellipse | `a1e97d2` |
| *(Not a bug)* Humanoid GLB suspected of regressing / of having lost its rigged animations | — | Asset is **byte-identical** since `093b1fb` (never touched). It is rigged (42 bones, 1 skin) but ships **zero animation clips** — which is why `humanoid.js` drives the bones procedurally, as its own header says. All four source variants in `assets/marsapiens/mars-sim` (`humanoid`, `humanoid_2`, `humanoid_skeleton`, `humanoid_skeletoned`) also have `animations: 0` and the same 42-bone skeleton, so swapping gains no animation and costs 10–16× size (0.32 MB → 3.2–5.2 MB). Every defect was in our procedural code | — (audit) |
| *(Not a bug)* `walker-rig.js` audited for the same defect | The plan-25 walkers generalize the Wave-12 humanoid IK, so the flawed hip term could have propagated | Confirmed clean — `walker-rig.js` already uses the correct `acos((L1²+R²−L2²)/(2·L1·R))` form. Ongak/Makadane unaffected | — (audit) |

## 2026-07-20 (plan 25 — legged walkers)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| New quad/octopod feet slammed ~1.1 m sideways at every touchdown | The swing trajectory reused from the humanoid, `swingT=(sin+1)/2` → `lift=swingLift·cos(s·π/2)`, inverts the arc (lift peaks at the stance transitions, zero mid-swing) and never lands the foot at the plant point; the biped hides it with world-locked stance feet + fast body, but a slow multi-leg body shows the slam | Continuity-planted swing: lerp the foot from its recorded lift-off point to a `stepHalf`-ahead home over a correct mid-swing `sin()` lift, and capture the stance plant from the foot's *actual commanded position* at touchdown — no jump possible by construction | `69751ab` |
| Diagonal-pair legs floated ~1.4 m in the air, never reaching ground | A leg whose cycle *begins* in swing (stride 0) never sees a lift-off edge, so its `swingFrom` stayed at world-origin `(0,0,0)`; the swing lerped from the origin, driving the IK target out of reach → foot folds up near the hip | Seed `swingFrom` (and `cmd`) in `plantIdle` so an already-in-swing leg has a valid on-ground origin before the first stride | `69751ab` |
| Headless verify: `__mc` never appeared, boot looked hung for 90 s | The bare game URL now opens the **globe hub** (plan 22) where `__mc` is only built after a site is entered; the old deep-boot assumption was stale | Deep-link straight into a site: `index.html?site=jezero` (append cache-bust with `&`, not `?`) | — (test method) |

## 2026-07-18 (van.glb landing + Wave 12.10–12.17)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Van floated half a metre above the terrain once van.glb swapped in | `CLEARANCE` was sized to the procedural fallback's floating origin (0.8, later 0.5), but both the GLB (models.js grounds base at y=0) and the new wheel-rig fallback put wheel contact AT the origin — the constant became pure hover | `CLEARANCE = 0.12` (rover's micro-relief margin); dock-ring y derives from it | `b7cfda5` |
| Stowed solar panels hid the entire textured GLB hull behind two blue slabs | Stow pose folded the wings 90° DOWN the hull sides — invisible on the boxy procedural fallback, a curtain on the real body | Wings stow flat ON the roof (180° hinge swing, cells down), staggered hinge heights so the overlapping wings don't z-fight; hinges re-seat on the measured GLB roof at swap | `b7cfda5` |
| Repair-rate E2E read as "repair not running" at 10s wall-clock windows | SwiftShader runs the sim at a few frames/second with dt clamped at 0.1 — 10 wall seconds is ~3 sim frames, so the deltas drowned in noise | Measure across a fixed rAF-tick count instead of wall time (25 frames → field/base = 0.40 exactly) | — (test method) |

**Landed features (all E2E-verified same day):** van.glb (textured Tripo source compressed 1.69 MB→671 KB, six-wheel overlay rig, nose orientation verified by vertex clustering) `b7cfda5`; field repair at the deployed van at 0.4× `dba9225`; ESTABLISH A FORWARD BASE survey step `87b8069`; recon photo mission with real frame captures `f02ea17`; van roof landable deck that carries landed drones `d3a5256`; buried subsurface core (survey-localized, humanoid-drill-only) `bbe53c2`; van cargo bay `77b795b`; recon sensor cone `04e0013`.

## 2026-07-17 (Wave 12 verification — van, gait, digging)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Boarding the van froze the game loop (every subsequent frame threw) | `van.js` update() used `_tiltQuat` (3 references) but the declared local is `_tiltQ` — ReferenceError on the van's first update, which only fires once the van becomes active, so boot looked healthy | Renamed to the declared `_tiltQ` | `3c4666a` |
| Pressing V (deploy) threw `units is not defined`; deploy was unreachable, making the entire mobile-base feature dead code | The `mc-van-deploy` listener was added at module scope next to `mc-menu`, but reads `units`/`activeIndex`/`van` — all `startGame()` locals. Every other key routes through a DOM button whose handler closes over that scope | New HUD `DEPLOY BASE / PACK UP BASE` button (gear-slot position; van has no gears so they never collide) wired to an in-scope `onVanDeploy`; the V key clicks the button — which also gives touch players deploy access for the first time | `3c4666a` |
| Van invisible at boot — hung at y=0, ~2.5 km above Jezero's −2567 m terrain | Ground units only update while active, and the one-shot boot grounding (`rover.update(0)`, `humanoid.update(0)`) never got a van line. Deploy easing also only advanced while the van was selected | `van.update(0, …)` at boot + idle zero-input update each frame (drone idiom): terrain-follow settles and the deploy animation finishes even if the player Tabs away mid-fold | `3c4666a` |
| After boarding the van, WASD drove an invisible, stowed humanoid; the van needed a manual Tab and the "active" camera followed a hidden unit | Mount stowed/hid the humanoid but never moved `activeIndex` to the van (plan contract: "active unit becomes the van"); dismount had the mirror bug | Mount/dismount set `activeIndex` + `applyUnitMode()`; dismount also probes 5 bearings for a non-colliding spot before teleporting the humanoid out | `3c4666a` |
| Stowed humanoid left an invisible 0.35 m obstacle at the mount point that blocked the van/rover | The collider registry entry stays live regardless of stow state | `enabled: () => bool` gate on collider unit entries; stowed humanoid also dropped from GPS bearings and minimap dots, and charges off the van's cabin dock while riding | `3c4666a` |
| First drill frame killed the render loop — dig banner froze, core never completed, and every downstream system (missions, scouting) went dead | main.js calls `effects.spawnDust()` during the dig, but effects.js never exported it (`spawnDust` was internal-only); the pageerror killed the rAF chain | Exported `spawnDust`; dust rate matched to the rover's wheel budget (1/frame) | `3c4666a` |
| Humanoid spine/head counter-lean on slopes was invisibly small (~6% of spec) | `currentPitch * (1 - Math.exp(-4*dt))` was written as a smoother but has no accumulator — it's a per-frame ATTENUATOR (×0.064 at 60 fps) | Real low-passed `smoothPitch` state; counter-lean now also live while standing still on a grade | `3c4666a` |
| Dig crouch froze on the rig forever if the core ended while standing still | Posture loop gated on `walkAmt > 0.01 \|\| digging` — after `cancelDig()` at standstill both are false, so the bones were never written back toward bind | `digAmt` 0→1 cross-fade drives the crouch in AND back out; loop gated on it | `3c4666a` |
| Foot IK skated: only 8% of walk frames had a world-locked stance foot | Plant lead was `0.45×STEP_HALF` (0.16 m) but stance covers `speed×π/STRIDE_RATE` ≈ 0.73 m — the foot left leg reach mid-stance and the IK clamp dragged it. Swing arc endpoints (±STEP_HALF) also didn't meet the plant point → visible pop | Full `STEP_HALF` plant lead: plant-to-release span (0.7 m) now matches stance travel; measured 27% locked frames with 10-frame holds and no transition pop | `3c4666a` |
| Stopping mid-stride froze one boot in the air; jumping from standstill froze the last IK pose for the whole flight | IK/swing loops gated on `walkAmt` — below 0.01 nothing writes the leg bones, so they hold whatever the last solve left | Idle IK plants both boots flat on their own terrain patch (rubble read while standing, feet pivot when turning in place); bind restored once when IK disengages (airborne) | `3c4666a` |
| Pelvis sway shifted the hips AFTER the feet were solved (one-frame foot drift) | Posture loop ran after the IK block, invalidating the parent matrices the solve had used | Rig order now swing → posture → IK; leg bones cached at rig build (no per-frame `rig.find`), dead `_ikHip` reads removed | `3c4666a` |
| Switching units mid-core left the drill banner stuck and the drill whine humming | Dig block only runs while the humanoid is active; an inactive humanoid never updates, so `digging` stayed true forever | `switchUnit()` aborts the core (cancel + banner clear + drill 0) — switching away is a deliberate act, same as moving | `3c4666a` |
| Scouted-devil map intel grew unbounded and stale markers drew at full strength for 3 sols | `scoutedDevils` map never pruned; minimap ring had no age signal | Pruned past FADE_SOLS in `scout()`; `getScoutedDevils` returns `age` 0→1 and the minimap ring fades 65% over it | `3c4666a` |
| Survey step read "SCAN ALL SCOUT ZONES" with no progress feedback across 3 zones | Step text is static; the zone count only lived inside `missions.advance` | Objective banner appends a live `(n/3)` tally while the scan step is current | `3c4666a` |

## 2026-07-17 (Wave 9.5/10 bug-hunt — placement, collision, hologram, survey)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Repair bay overlapped its chargepad at **every** base; one bay overlapped a checkpost; a mast clipped a bay (11 overlapping pairs measured across a full build-out) | Bay placed at a fixed 5.5m/45° offset from pad center with no rock/occupancy check — `chargepad.glb` scales to a 10m footprint (r≈5.2), so a 6m workshop at 5.5m sits ON the deck. Bay was also invisible to `blockedAt`, so masts/HQ could rise on it. `blockedAt`'s pad clearance (4.6) and the pad-siting probe (4.6) were still sized to the old 3.6m procedural disc | Bay sited on its own ring (10.5m, flattest-of-8, rocks + `blockedAt`) and registered as a static collider (r2.8 h4) so later structures dodge it; `blockedAt` pad clearance 4.6→6.5; pad placement probes at the GLB radius (5.8); checkpost pad ring 9→11, mast 12→13. Re-audit: 33 structures, **0** overlapping pairs | `7a7d1d1` |
| Rover/humanoid drove and walked straight through the repair bay | The bay was never `addStatic`-registered — a building with no collision body | Static collider at the bay spot; verified blocked-entry: rover stops 4.2m from bay center (r_bay+r_rover), humanoid 3.15m | `7a7d1d1` |
| Units sank into the chargepad platform (drones buried 0.9m to the belly) and clipped through the 0.35m lab pad; driving "onto" a pad meant driving *through* it | Every unit's ground clamp = terrain height only. The chargepad GLB is a raised 0.9m deck (raycast-measured 0.88m, flat to r≈3.6) — nothing modeled platforms as ridable surfaces | New drivable-deck registry in colliders.js (`addDeck`/`deckHeight`, ramped edges keep ground-rise below the rover's launch threshold). Rover, humanoid, both drones, sling loads, field drops and the tether anchor now ground on terrain+deck. Deck height/radius re-measured from the real GLB at swap (raycast), and the rim solar panels are auto-clustered into small static colliders — blocked, not clipped | `7a7d1d1` |
| Ariana's dialog never displayed — hologram triggered but no line ever appeared | main.js's objective-banner branch ran `hud.setObjective(null)` **every frame** while `hologram.active` (the "dialog preempts the banner" logic), erasing each scripted line the same frame the hologram callback set it | While the dialog is active the banner slot is left alone; the mission banner resumes after the last line (verified: lines display, banner returns to `1/7 · DRIVE…`) | `7a7d1d1` |
| Ariana hologram embedded inside the station building | Spawned at `lab.stationPos` — the dock's *center* — with the pad's y | Placed in front of the dock (+3.2, +5.6 past the box's 3.75m half-width), grounded on her own terrain sample | `7a7d1d1` |
| Station dock floated above the terrain | `stationGroup` was grounded at the *pad's* terrain height; the dock sits 14.5m west, where Jezero drops ~0.4m | Grounded on its own sampled height; `stationPos.y` follows, so the landing-intro touchdown still matches exactly | `7a7d1d1` |
| Survey mission scan progress silently frozen while the tutorial was active | main.js gated scan advance on `missions.currentAny()?.missionId === 'survey'`, but `currentAny()` returns the FIRST active chain in insertion order — the autostarted tutorial | Gate on `missions.activeMissions.includes('survey')` (the broadcast design already handles multiple chains); minimap survey ring uses the same flag | `7a7d1d1` |
| Survey "RETURN TO BASE" completed by merely overflying the pad at cruise height (and only while the recon was the active unit) | `chargepads.padAt()` is a horizontal-distance test; the step also wrongly required the recon to be selected | Requires `recon.landed && padAt(recon.position)`; active-unit requirement dropped | `7a7d1d1` |
| Antenna mast collider thinner and shorter than the real tower | Static registered r1.2/h9 for the procedural pole; `antenna.glb` has a 2.9–3.2m guy-leg spread and stands 9.24m | r1.6, h9.5 | `7a7d1d1` |

## 2026-07-14 (Wave 8 — playtest follow-ups)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Mars HQ (origin base) hard to pick out on the minimap | HQ and checkposts were both teal squares differing only in size/fill (14px solid vs 8px hollow, same `#59d8c9`). A size-only cue is unreadable at the 140px CSS map size, on a canvas already carrying teal squares (field lab), amber circles (samples), orange diamonds (caches) and white unit dots. The in-world beacons deliberately share one cyan (Wave 7 found an amber column washed the white hull gold), and that "no colour split" decision had been carried into the minimap where it does not apply | HQ now draws as a **haloed star** (`starPath()` in fog.js) — bright teal core, white outline, soft pulsing halo. Stays in the teal "base" family for semantics; shape carries the distinction. Live-canvas probe: 66 bright px at the HQ vs 0 at a checkpost, 3.2× lit-pixel density | `025762b` |
| GEAR button overlapping the telemetry card (desktop) | Telemetry is bottom-anchored and **231px tall**, so its top edge lands 243px above the viewport floor — but GEAR was pinned at `bottom: 215px`, i.e. *inside* the card's box. Its lower 28px sat on top of the card (123×28px overlap measured) | GEAR moved to `bottom: 285px`, clearing the measured card height with margin for a wrapped site line | `025762b` |
| GEAR button overlapping the compass (mobile) | The mobile media query moved GEAR to `top: 222px` but left the base rule's `right: 12px` — putting it in the same right rail as the compass (`top: 218px`, 46px tall), a 46×30px direct hit. The compass has no mobile override at all, so nothing moved out of its way | Right rail re-stacked in order: minimap (70-212) → compass (218-264) → gear (272-302) → drone control board (310+, was 270 and would have collided with the relocated gear). Zero overlaps re-probed at 390×844, incl. the expanded drone board | `025762b` |

## 2026-07-12 (Wave 4 playtest fixes)

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Drone battery drained in 1-2 min (faster with cargo) — real cargo drones last 20-30 min | Drain rates were tuned for gameplay urgency, not realism: lift `drainRate 0.5`, recon `0.35`, plus `SLING_DRAIN 1.6` halved laden endurance. Lift airborne floor (load 0.4) × 0.5 × 1.5 (G2) ≈ 0.3 %/s → ~5 min hover, far less under input/cargo | Recalibrated to real endurance: lift `0.11` / recon `0.10` / rover `0.05` / humanoid `0.07`; `SLING_DRAIN` 1.6→1.35; `SOLAR_RATE` 0.6→0.25 (proportional recharge). Measured: lift idle hover **25 min**, laden **19 min** to empty | `3989243` |
| Rover started/stopped instantly — no vehicle-like momentum | `speed = throttle × …` was assigned directly each frame, so releasing the key set `speed = 0` that instant (pure kinematic, zero inertia) | Actual speed now eases toward a throttle-derived TARGET with a time constant (`ACCEL_TAU 0.9` / faster `BRAKE_TAU 0.6` for coast-down), the same target-velocity idiom the drone already uses — rolls up and coasts to a stop; imperceptible below G1 so REAL gear stays 1:1 | `3989243` |
| Telemetry data card "vanished" on desktop | `mc-tele=collapsed` persisted from an earlier touch/mobile session, but the un-collapse toggle only renders inside the coarse-pointer CSS — desktop applied the collapsed state with NO control to reopen it (card shrank to the site-name line) | Collapse pref now applies only on touch devices (`isTouchDevice() &&` gate in hud.js); desktop always renders the full card | `3989243` |
| Lift drone could not hook a cache while landed next to it | `tryCollect`'s sling gate required `!unit.landed` — only a low HOVER could hook, though a parked drone beside the cache is the natural real-world pickup | Gate is now `alt <= SLING_ALT` alone (landed alt=0 passes); the load sits beside the drone and lifts off with it on take-off; SLING prompt shows while parked too | `3989243` |
| Slung cache dragged along the ground in flight — "rope too long" | `CABLE_LEN` 4.2m: at typical low-AGL flight the ground-clamp kept engaging, the load scraped terrain and read as drag. Real cargo drones keep slung loads on SHORT lines for control authority (winch systems snug the payload under the airframe for cruise; long lines are a helicopter-longline technique) | `CABLE_LEN` 4.2 → 2.2m — at 12m cruise the load rides ~9m AGL, clamp only engages at genuinely unsafe heights | `3989243` |
| Cache pickup points invisible on the minimap | Field containers were never passed to `fog.render` — only sample markers (fog-gated) and unit dots were drawn | New `extras.caches` layer: orange diamonds (container lid color) above the fog for every `state === 'field'` container, cleared on pickup/delivery | `3989243` |
| No compass — nose direction only readable from the HDG number | Minimap's active-unit heading tick was the only visual cue, too subtle at 140px | Compass dial docked under the minimap: fixed N ring + needle rotating to the active unit's nose bearing (10Hz, no CSS tween — a transition would spin the long way on the 359→0 wrap) | `3989243` |
| Landing-drop intro appeared to hang with no way out on slower machines | Drop timer accumulated **clamped sim-dt** (≤0.1/frame): during station-GLB decode + full-terrain view from 500m, fps tanked and 7.5s of sim time stretched to 30–60s wall-clock with all input suppressed; the only skips were click/button — no keyboard escape | Intro now runs on wall-clock time (`performance.now`), always ~7.5 real seconds at any fps; ANY key or click/tap skips | `8087e8c` |
| Station dock interior showed red terrain instead of container surfaces | The Tripo container GLB has window/door openings and single-sided walls — interior backfaces were culled, exposing the terrain behind the shell | `doubleSide: true` per-model flag in `STATIC_MODELS`; `attachStaticModel` sets `THREE.DoubleSide` on the material | `8087e8c` |
| Station dock read rover-sized — nowhere near "houses the fleet" | `footprint: 6` scaled the GLB to the old dome's ~6m footprint | Footprint 6→15m hangar scale; collision circle deliberately updated (r7.6/h8.5 at `PAD_RADIUS+9`), mast moved beside the dock, dock footprint added to the lab flat-spot rock check | `8087e8c` |
| Drop cinematic was narratively backwards: dock already built on the ground while a drone descended | Intro dropped the lift-drone mesh (cheapest body) next to a pre-placed base | The STATION CONTAINER itself cargo-drops onto its exact resting spot — real dock hidden during descent (re-hidden per frame to beat the async fallback-reveal race), revealed at touchdown; procedural parachute canopy + shroud lines, jettisoned at 85% descent | `8087e8c` |
| Camera sat inside the descending container's hull during the drop | Chase-rig distance stays at its 12m boot default — fine for a 2.5m drone, inside the geometry of a 15m building | `camRig.setDistance()` added; intro frames at 70m, restored to 12m on handoff | `8087e8c` |
| Tutorial read as a single lonely hint; no sense of a step chain | Only the current step rendered, with no progress indicator and no upfront overview | All-steps overview card at tutorial start (START/SKIP, reshown on menu replay) + `1/7 ·` step counter in the banner | `8087e8c` |
| Intro/tutorial never played for returning players — and no way to rewatch | First-visit localStorage flags (set on start, by design) — but no replay path existed | MENU → ▶ LANDING INTRO / ▶ TUTORIAL replay both anytime without touching flags or resetting; `__mc.intro/tutorial` became live getters (plain refs went stale after replay) | `8087e8c` |
| Gray fallback blocks flashed for ~1s at boot before GLBs swapped in | Fallback-first design renders procedural primitives immediately while GLBs decode | `deferFallback()` hides fallbacks 2.5s — fast loads swap in invisibly, slow/failed loads still reveal the fallback (offline guarantee kept) | `8087e8c` |

## 2026-07-11

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Rover (and every other unit) drove straight through the FIELD LAB dome, airlock and the other units | Only rock collision existed — `rocks.collides()` was the sole obstacle test; lab structures and units had no footprints anywhere | New `colliders.js` registry: static circles (dome/airlock/mast from `lab.obstacles`) + live unit-vs-unit circles, altitude-banded so cruising drones overfly the 3.1m dome but can't fly through it; each unit moves against a `forUnit()` facade that keeps the existing "blocked entry, never trapped" rule | `1f9f522` |
| Driving to the site edge "broke the renderer" (washed-out void, camera staring past the world) | No movement bounds: units left the finite terrain mesh; `sampleHeight` clamps at the DEM edge so the unit floated off-map with nothing to render | All units clamp to `worldSize/2 − 30m`; pushing the edge sets `atBoundary`, HUD flashes "⚠ OUT OF MISSION DIRECTIVES — RETURN TO SURVEY ZONE" (drones keep sliding along the edge — only the clamped axis' velocity zeroes) | `1f9f522` |
| Humanoid read toy-sized beside the rover despite being scaled to 2.2m (numerically taller than the rover GLB's 2.0m box) | Perceptual, not numeric: the Tripo rover GLB renders as a ~3.9m-square hulk (real Perseverance is 2.7m wide), so a 1.1m-wide figure still read small; skinned-mesh measurement confirmed the 2.2m was rendering correctly | Bumped humanoid to 2.6m (same readability-over-realism policy models.js already applies to the drones) | `1f9f522` |
| No way to restart a session (positions, batteries, samples, lab state) | No reset control existed | RESET MISSION button in the menu SIM section, two-step arm/confirm (4s window), reloads the page — all sim state is in-memory so a reload is a pristine restart; SCIENCE ARCHIVE + gear prefs persist by design | `1f9f522` |
| Idle airborne drone with an empty battery hovered forever at 0% | `dead` flag was computed only for the **active** unit in the main loop, so an idle drone draining to 0 never got `setPower(false)` force-landed until you switched to it | Dead-flag loop now covers every unit each frame | `1f9f522` |
| Collectibles: only 4–5 per site, all along one traverse line | Samples were literally the published rover traverse waypoints — real traverses are lines | Jezero 5→12, Gale 4→10: added real off-traverse features at derived coordinates (Kodiak, Three Forks depot, Belva, Máaz, Windjana, Buckskin, Bagnold Dunes, Murray Buttes — noted "approx. location") plus `[SIM]` survey targets spreading objectives across all four map quadrants | `1f9f522` |

## 2026-07-10

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Terrain invisible on both sites after the material-blend change (shader `VALIDATE_STATUS false`, caught by the headless smoke test before deploy) | `float patch = …` in the new fragment-shader block — `patch` is a GLSL reserved word (tessellation keyword, reserved in GLSL ES too), so the whole fragment shader failed to compile | Renamed to `patchN`; smoke test asserts zero console errors on both sites | `a820b19` |
| Lab delivery would have been unreachable in normal play: hovering over the pad at default cruise altitude showed RELEASE LOAD (field-drop) instead of DELIVER | `DELIVER_ALT` (10 m) was below the lift drone's `cruiseAlt` (12 m), so the deliver gate never opened at the altitude auto-take-off parks you at | Raised `DELIVER_ALT` to 16 m — arriving at cruise height delivers; the 4.2 m cable sells the lowering | `a820b19` |
| Rotor-node GLB re-export (Tripo/Blender) aborted: trimesh connected-component split ran the machine out of memory (session-killing) | No Blender on this box; `trimesh.split()` on the fused Tripo meshes ballooned memory during the probe | Dropped mesh surgery entirely — procedural rotor overlay (`rotors.js`) with hubs derived from each GLB's bounding box; revisit real re-export on a bigger machine | `a820b19` |

## 2026-06-22

| Issue | Root Cause | Resolution | Commit |
|---|---|---|---|
| Colony Stats deep-dive chart rendered blank (axes only, no lines) | Chart.js 4 `time` scale needs a date adapter; none was loaded, so the time axis silently failed to plot points | Vendored `chartjs-adapter-date-fns.bundle.min.js` and loaded it after `chart.umd.min.js` | `6410c15` |
| Summary cards showed unit/trend but no value (e.g. blank where `100%` expected) | `fmt(v, metric)` multiplied by 100 assuming 0–1 fractions, but ClickHouse stores display-ready values (`battery_soc_pct = 100`, not `1.0`); it also mis-scaled non-percent metrics like `dust_opacity` (tau) | Removed the `×100` heuristic; all values now formatted via `fmtNum` directly | `6410c15` |
| Only 6 of 13 domains appeared on the dashboard | Exporter queried `tags['tier']='macro'`, which only exists for 6 causally-modeled domains; the other 7 are micro-tier only | Rewrote exporter to read the `micro` tier from `ef.telemetry_1m` (covers all 13 domains, 38 metrics); page presents them as per-entity telemetry | `6410c15` |
| Mixed-magnitude metrics flattened to invisible lines (e.g. `dust_opacity` ~0.3 vs `pressure_pa` ~900 on one Y-axis) | Single shared linear Y-axis compresses small-magnitude series to the baseline | Added AUTO-AXIS (auto-splits series onto left/right axes when magnitude span > 50×, marks right-axis series with `(R)`) and a NORMALIZE mode (rescales each series to 0–100% of its own range) | `6410c15` |
| `argMax(sumMerge(sum_v)/countMerge(cnt), bucket)` rejected by ClickHouse (`ILLEGAL_AGGREGATION`) | Cannot nest an aggregate (`sumMerge`) inside another aggregate (`argMax`) | Computed per-bucket avg in a subquery first, then `argMax(bucket_avg, t)` over the subquery for the "last value" | `6410c15` |
